// docs/xml-pc-generator/guide/data/pages.js
// 서술형 챕터 본문. 키는 nav.js의 챕터 id.
// h2/h3가 우측 목차로 자동 수집되므로 섹션은 반드시 h2로 연다.
window.PAGES = {
  intro: `
    <h2>1.1 Figma 기반 화면 생성 원리</h2>
    <p class="lead">본 가이드는 Figma 디자인이 변환기를 거쳐 WebSquare 화면으로 전환되는 프로세스를
    다룹니다. 해당 과정은 수작업 재설계 없이 자동화된 흐름으로 진행됩니다.</p>
    <p>변환기는 사전 정의된 규칙에 따라 작동하는 프로그램으로, 레이어 <b>이름</b>·<b>클래스</b>·<b>중첩 구조</b>의
    세 가지 데이터만을 기반으로 화면을 구성합니다. 세 데이터에 명시되지 않은 정보는 추론하지 않습니다.</p>
    <p>특히 레이어 이름은 변환기가 각 컴포넌트의 유형을 식별하는 유일한 기준입니다.
    명명 규칙을 준수해야 정확한 컴포넌트 변환이 가능하며, <b>규격에 맞지 않는 이름은 의도와 다른
    컴포넌트로 변환되거나 변환 대상에서 제외됩니다.</b></p>
    <p>본 가이드는 기존 디자인 방식을 크게 변경하지 않고도 적용할 수 있는 핵심 규칙을
    단계별로 안내합니다.</p>

    <h2>1.2 역할 분담</h2>
    <p>화면 하나가 생성되기까지의 책임은 세 주체로 구분됩니다.</p>
    <div class="tblwrap">
      <table>
        <thead>
          <tr><th>담당</th><th>책임지는 것</th><th>참조</th></tr>
        </thead>
        <tbody>
          <tr><td><b>디자이너</b></td><td>레이어 이름, 클래스, 구조</td><td>2~4장</td></tr>
          <tr><td><b>변환기</b></td><td>셀 속성·표 뼈대 등 구조 보강</td><td>4장</td></tr>
          <tr><td><b>CSS</b></td><td>색과 치수</td><td>3장(클래스)이 연결 지점</td></tr>
        </tbody>
      </table>
    </div>
    <p>디자이너가 정의하는 대상은 <b>이름·클래스·구조</b>로 한정됩니다. 색상 일치나 픽셀 단위
    치수 조정은 CSS가 담당하므로 디자이너의 작업 범위에 포함되지 않습니다. 다만 어떤 레이어에
    어떤 클래스를 지정했는지는 CSS가 값을 적용할 대상을 식별하는 유일한 기준이므로 정확하게
    작성해야 합니다.</p>
    <p>변환기도 일부 구조를 보강합니다. 표의 칸이 헤더인지 여부, 병합된 칸 수 등의 속성과
    행·칸을 연결하는 뼈대는 별도로 작성하지 않아도 변환기가 자동으로 생성합니다. 자동 생성
    대상의 전체 목록은 4장에 정리되어 있습니다.</p>
    <div class="note">세 요소 중 하나가 누락되어도 화면 생성 자체는 실패하지 않으며,
    <b>의도와 다른 결과</b>가 생성됩니다. 본 가이드의 상당 부분은 이러한 결과 불일치가
    발생하는 지점을 사전에 안내하는 데 할애되어 있습니다.</div>

    <h2>1.3 두 가지 작업 유형</h2>
    <p>작업 유형은 <b>디자인 템플릿을 복제해 수정하는 경우</b>와 <b>신규 디자인을 작성하고 자체 CSS를
    적용하는 경우</b>로 구분됩니다. <b>적용 규칙은 두 유형이 동일하며</b>, 2~5장의 내용은 작업 유형에
    따라 달라지지 않습니다. 차이는 <b>오류가 발생하는 지점</b>에 있습니다.</p>
    <ul class="pts">
      <li><b>템플릿 복제</b> — 이미 규격에 맞게 작성된 이름·클래스가 내용 수정 과정에서 변경되어
      어긋납니다. 텍스트만 교체하려다 레이어를 삭제하고 재생성하면서 이름이 누락되는 경우가
      대표적입니다. <b>수정 시 주의 대상</b>을 중심으로 확인합니다.</li>
      <li><b>신규 디자인</b> — 표준 이름·클래스를 적용하지 않은 상태에서 시작하므로 <b>초기부터
      규격과 불일치</b>합니다. <b>사전에 맞춰야 할 항목</b>을 중심으로 확인합니다.</li>
    </ul>

    <h2>1.4 작업 전 확인 사항</h2>
    <p>2장 이후의 내용을 적용하기 전에 작업 유형에 따라 선행할 항목이 있습니다.</p>
    <div class="warn"><b>자체 디자인 시스템을 운용 중인 경우, 클래스 이름을 표준 규격에 맞추는
    작업이 선행되어야 합니다.</b> 3장에서 다루듯 클래스 이름은 정해진 목록의 값을 그대로 사용해야
    변환기가 인식합니다. 자체 명명 체계를 사용 중이라면 화면 작성 전에 표준 이름으로 전환합니다.
    <b>이름은 규약이며 값은 자체 정의 대상입니다</b> — 색상·여백 등 실제 값은 유지하고 이름만
    교체하는 작업입니다. 화면 작성을 완료한 뒤 전환할 경우 모든 레이어의 이름을 재수정해야 합니다.</div>
    <p>해당 작업이 완료되었거나 처음부터 표준 클래스만 사용할 예정이라면 이후 내용을 순서대로
    적용합니다. 2장 이름, 3장 클래스, 4장 구조, 5장 크기와 오토레이아웃 — 이 네 개 장은 화면
    종류와 무관하게 항상 적용되는 공통 규칙입니다.</p>

    <h2>1.5 기대 결과</h2>
    <p>본 가이드의 목표는 다음과 같습니다. <b>가이드의 규칙을 준수해 작성한 화면은 디자인 템플릿과
    동일한 결과로 변환됩니다.</b> 템플릿을 복제하지 않고 신규로 작성한 화면이라도 이름·클래스·구조·크기
    규칙을 준수했다면, 변환 결과는 템플릿과 동일한 방식으로 동작하는 WebSquare 화면입니다.</p>
    <p>이후 장의 구성은 다음과 같습니다.</p>
    <ul class="pts">
      <li><b>2~5장</b> — 화면 종류와 무관하게 항상 적용되는 공통 규칙 네 가지(이름·클래스·구조·크기).</li>
      <li><b>6~18장</b> — 조회영역·탭·표·그리드 등 컴포넌트별 공통 규칙 적용 방법.</li>
      <li><b>19~20장</b> — 전달 전 확인 목록과 결과 불일치 시 원인 식별 방법.</li>
      <li><b>21장</b> — 부록.</li>
    </ul>
  `,
  naming: `
    <h2>2.1 이름 짓는 법</h2>
    <p class="lead">레이어 이름은 <b>밑줄(<code>_</code>)로 구분한 3단어</b>로 짓습니다.
    <code>접두어_컴포넌트_클래스</code> 순서입니다.</p>
    <p>예를 들어 조회 조건을 담는 영역이라면 <code>search_group_schbox</code>처럼 짓습니다.
    앞의 <code>search</code>는 "이게 뭐 하는 영역인지" 사람이 알아보라고 붙이는 접두어이고,
    가운데 <code>group</code>이 변환기에게 "이건 그룹 컴포넌트다"라고 알려주는 이름이고,
    뒤의 <code>schbox</code>는 그 컴포넌트에 입힐 클래스(모양)입니다.</p>
    <div class="warn"><b>가운데 자리에 들어갈 수 있는 값은 정해진 컴포넌트 목록(부록 21.1)뿐입니다.</b>
    <code>schbox</code>·<code>tblbox</code>·<code>lybox</code>처럼 <b>영역의 이름처럼 보이는 것들은 컴포넌트가 아니라
    클래스</b>라서, 가운데가 아니라 뒷자리에 적어야 합니다. 조회영역·입출력 테이블·레이아웃은
    전부 <b>컴포넌트로는 <code>group</code></b>이고, 무엇인지는 클래스가 정합니다.</div>
    <ul class="pts">
      <li><b>접두어</b> — 의미 식별용이며 변환기는 참조하지 않습니다. 자유롭게 지정할 수 있습니다.</li>
      <li><b>컴포넌트</b> — 변환기가 유형 판정에 사용하는 값으로, 본 장의 핵심 대상입니다.</li>
      <li><b>클래스</b> — 컴포넌트 확정 후 해당 컴포넌트에 적용할 형태를 지정합니다.
      가운데 이름 뒤의 모든 값이 클래스로 처리됩니다.</li>
    </ul>
    <div class="note">클래스 지정 규칙은 3장에서 다룹니다. 본 장은 가운데 조각, 즉 컴포넌트 값을
    정확히 지정하는 방법을 다룹니다.</div>

    <h2>2.2 컴포넌트 조각의 일치 규칙</h2>
    <p>변환기는 레이어 이름을 <code>_</code> 기준으로 분리한 뒤 <b>두 번째 조각</b>을 컴포넌트 목록과
    <b>완전 일치</b> 방식으로 비교합니다. 한 글자라도 다를 경우 다른 값으로 처리됩니다. 두 번째 조각이
    정확히 일치하면 이후의 모든 조각은 클래스로 처리됩니다.</p>
    <p>예를 들어 컴포넌트를 <code>group</code>으로 지정하려는 경우 <code>gro_up</code>·<code>groups</code>·
    <code>group1</code>은 모두 다른 값으로 인식됩니다. <code>_</code>의 위치, 글자의 추가·누락, 단어 변경은
    모두 별개의 값으로 처리됩니다. 단 <b>대소문자는 구분하지 않으므로</b> <code>Group</code>·<code>GROUP</code>은
    <code>group</code>과 동일하게 인식됩니다.</p>
    <div class="note"><b>단일 단어로 이름을 지정하지 않습니다.</b> <code>th</code>·<code>td</code>와 같은
    단일 단어 이름은 분리 기준인 <code>_</code>가 없어 두 번째 조각이 존재하지 않습니다.
    <code>cond_th</code>·<code>field_td</code>와 같이 접두어를 포함해 최소 두 조각으로 지정합니다.</div>

    <h2>2.3 규격 불일치 시의 동작</h2>
    <p>본 장의 핵심 사항입니다. <b>컴포넌트 조각이 목록과 일치하지 않아도 변환은 실패하지 않습니다.</b>
    변환기는 레이어 이름 <b>전체</b>에서 인식 가능한 단어를 탐색하고, 해당 단어가 존재하면 그 값으로
    컴포넌트를 추정합니다.</p>
    <p>이 추정은 <b>별도의 경고 없이</b> 수행됩니다. 매칭에 완전히 실패해 컴포넌트가 비어 있는 경우는
    화면에서 즉시 식별되지만, 다른 컴포넌트로 추정되어 화면이 채워지면 작성자와 인수자 모두 불일치를
    인지하기 어렵습니다. <b>미매핑보다 오매핑의 위험도가 높습니다.</b></p>
    <p>다음은 실제 매칭 로직으로 확인한 사례입니다.</p>
    <div class="tblwrap">
      <table>
        <thead>
          <tr><th>레이어 이름</th><th>변환 결과</th><th>매칭된 단어</th><th>작성 의도</th></tr>
        </thead>
        <tbody>
          <tr><td><code>context_area</code></td><td>텍스트박스</td><td><code>text</code></td><td>설명 영역</td></tr>
          <tr><td><code>calendar_area</code></td><td>입력달력</td><td><code>calendar</code></td><td>달력을 담는 영역</td></tr>
          <tr><td><code>selected_item</code></td><td>셀렉트</td><td><code>select</code></td><td>선택된 항목</td></tr>
          <tr><td><code>tab_content_area</code></td><td>탭컨트롤</td><td><code>tab</code></td><td>탭 콘텐츠 영역</td></tr>
        </tbody>
      </table>
    </div>
    <div class="note"><b>컴포넌트 조각이 목록과 일치하면 폴백은 동작하지 않습니다.</b>
    <code>calendar_group</code>은 두 번째 조각이 <code>group</code>이므로 그룹으로 확정되며 접두어의
    <code>calendar</code>는 무시됩니다. 폴백은 <b>컴포넌트 조각이 불일치할 때만</b> 동작하는 보조 경로이며,
    위 표의 사례는 모두 두 번째 조각이 목록에 없는 경우입니다.</div>
    <p>위 네 건은 모두 작성 의도와 다른 결과입니다. <code>context_area</code>는 설명 영역으로 작성되었으나
    이름에 <code>text</code>가 포함되어 텍스트박스로 변환되었습니다. <code>selected_item</code>과
    <code>tab_content_area</code> 역시 의미와 무관하게 <code>select</code>·<code>tab</code> 문자열만으로
    매칭되었습니다.</p>

    <h2>2.4 피해야 할 단어</h2>
    <p class="lead">2.3의 오매핑은 <b>중복 단어를 사용하지 않는 방식</b>으로 예방합니다. 다음은 변환기가
    인식하는 단어 중 <b>레이어 이름에 사용될 가능성이 높은 값</b>으로, 접두어와 클래스 자리에서 사용을
    제외해야 하는 대상입니다.</p>
    <div class="warn">
      <p><b>영어</b> — <code>text</code> · <code>tab</code> · <code>grid</code> · <code>tree</code> ·
      <code>node</code> · <code>input</code> · <code>select</code> · <code>group</code> ·
      <code>table</code> · <code>image</code> · <code>img</code> · <code>radio</code> ·
      <code>calendar</code> · <code>button</code> · <code>anchor</code> · <code>slider</code> ·
      <code>secret</code> · <code>spinner</code> · <code>trigger</code> · <code>acd</code></p>
      <p><b>단어 내부에 포함된 경우에도 매칭됩니다.</b> <code>context</code>에는 <code>text</code>가,
      <code>selected</code>에는 <code>select</code>가 포함되어 있습니다. 이름의 위치와 무관하게 매칭됩니다.</p>
    </div>
    <div class="note"><b>표 구조 관련 세 단어는 예외 처리됩니다.</b> <code>tr</code>·<code>th</code>·<code>td</code>는
    길이가 짧아 <b>이름 조각과 완전히 일치할 때만</b> 매칭되도록 별도 처리되어 있습니다. 따라서
    <code>star</code>·<code>track</code>과 같이 <code>tr</code>이 포함된 이름은 매칭되지 않습니다.</div>
    <p>컴포넌트 조각에는 2.2의 규격에 맞는 값만 사용하고, 그 외의 의미 전달은 위 단어를 제외한
    접두어 또는 클래스에 작성합니다. <b>가독성이 높은 단어일수록 중복 가능성이 높습니다.</b>
    전체 키워드 목록은 21장 부록에 있습니다.</p>

    <h2>2.5 이름에 적는 상태와 변형</h2>
    <p>동일한 컴포넌트라도 비활성화·읽기전용·필수·에러 등의 상태나 형태 변형이 있는 경우 이름에
    명시해야 합니다. 색상만으로 표현한 상태는 변환기가 인식하지 못합니다.</p>
    <ul class="pts">
      <li><b>공통 상태</b> — <code>disabled</code>(비활성화), <code>readonly</code>(읽기전용),
      <code>req</code>(필수입력), <code>error</code>(에러 상태)</li>
      <li><b>셀렉트 계열</b> — <code>selectnative</code>(기본 셀렉트박스), <code>selectsel</code>(커스텀 셀렉트)</li>
      <li><b>캘린더 계열</b> — 날짜 단위(연월만 받을지, 초까지 받을지)를 이름으로 정합니다.
      단위별 이름 목록은 16장 「캘린더」에서 다룹니다.</li>
    </ul>
    <p>상태 값 역시 컴포넌트 조각 뒤, 즉 클래스 자리에 작성합니다. 예를 들어
    <code>field_input_disabled</code>와 같이 지정합니다. 조합 규칙은 3장에서 다룹니다.</p>

    <h2>2.6 자동매핑 결과 확인</h2>
    <p>명명 과정에서의 오류를 보완하기 위해 변환 과정에는 확인 단계가 포함되어 있습니다.
    자동매핑은 판정 결과를 즉시 적용하지 않고, <b>각 레이어가 어떤 컴포넌트로 판정되었는지 모달로
    표시한 뒤 사용자 확인을 거쳐 적용</b>하는 방식으로 동작합니다.</p>
    <div class="note">2.3의 오매핑은 최종 적용 전 이 모달에서 확인할 수 있습니다. 전달 전에 해당
    목록을 검토하여 의도와 다르게 판정된 항목이 없는지 확인합니다. 세부 점검 절차는 19장에서
    다룹니다.</div>
  `,
  class: `
    <h2>3.1 클래스의 자리</h2>
    <p class="lead">클래스는 <b>레이어 이름의 두 번째 <code>_</code> 뒤에 오는 부분 전부</b>입니다.
    2장에서 본 <code>접두어_컴포넌트_클래스</code> 순서에서 맨 뒤 칸입니다.</p>
    <p>클래스가 복수인 경우 <b>공백으로 구분해 나열</b>합니다. 예를 들어 채움 형태의 검색 버튼은
    최종 클래스가 <code>btn_cm fill search</code>와 같이 세 개의 값으로 구성됩니다. 조합 방식은
    3.2에서 다룹니다.</p>
    <div class="note">두 번째 <code>_</code>의 위치는 2장에서 다룬 <b>컴포넌트 조각이 종료되는 지점</b>으로
    결정됩니다. 컴포넌트 값이 정확해야 클래스도 올바른 위치에서 분리되며, 컴포넌트 값이 불일치하면
    클래스도 잘못된 위치에서 분리됩니다.</div>

    <h2>3.2 클래스의 역할</h2>
    <p class="lead">본 절의 핵심 사항은 다음과 같습니다. <b>클래스를 비워 두지 않습니다.</b></p>
    <p>클래스가 지정된 경우 형태는 해당 클래스의 CSS가 정의합니다. 반면 <b>클래스가 없으면 기준이
    되는 정의가 없으므로, 해당 레이어의 너비·높이·색상이 Figma 측정값 그대로 XML에 인라인으로
    출력됩니다.</b> 화면상 결과는 동일해 보이지만 해당 값은 <b>CSS로 수정할 수 없는 상태로 고정</b>되며,
    이후 수정 시 레이어를 개별적으로 찾아 변경해야 합니다.</p>
    <div class="warn"><b>클래스를 미지정 상태로 남겨 두고 이후에 보완하는 방식은 권장하지 않습니다.</b>
    해당 레이어의 측정값이 그대로 결과에 고정되는 사례가 빈번합니다.</div>
    <div class="note"><b>지정하지 않은 클래스가 결과에 추가되어 있는 것은 정상 동작입니다.</b> 일부
    컴포넌트는 기본 클래스를 보유하며 이름에 지정한 값과 병합됩니다. 표에 <code>tbl</code>만 지정해도
    결과는 <code>w2tb tbl</code>이 됩니다.<br>
    기본 클래스 보유 대상은 다섯 가지입니다 — 표(<code>w2tb</code>) · 행(<code>w2tb_tr</code>) ·
    라벨셀(<code>w2tb_th</code>) · 값셀(<code>w2tb_td</code>) · 진행 단계(<code>processbar</code>).
    <b>이 다섯은 이름에 클래스를 지정하지 않아도 됩니다.</b> 그 외 컴포넌트는 이름에 지정한 값만
    적용됩니다.</div>

    <h2>3.3 표준 클래스 목록</h2>
    <p>본 절의 핵심 규칙입니다. <b>클래스 이름은 신규로 정의하지 않고 지정된 목록의 값을 그대로
    사용합니다.</b> 아래 표는 <b>디자인 템플릿에서 실제 사용 중인</b> 클래스 목록입니다.</p>
    {{CLASS_TABLE}}
    <div class="note">표에 포함되지 않았으나 사용 가능한 클래스가 존재합니다. 템플릿에서 사용하지
    않는 값이기 때문이며, 레이아웃 패널의 비율을 지정하는 <code>col_2</code>~<code>col_9</code>가
    이에 해당합니다(6장). <b>컴포넌트별 챕터(6~18장)에서 안내하는 클래스는 표에 없어도 사용할 수
    있습니다.</b> 반면 <b>표와 챕터 어디에도 없는 이름은 변환기가 인식하지 않습니다.</b></div>
    <p>버튼은 복수 클래스를 조합해 사용하는 대표 컴포넌트이며 조합 순서가 정의되어 있습니다.
    <b><code>btn_cm</code> → 변형 → <code>icon</code> → <code>disabled</code></b> 순서입니다.
    아이콘이 포함된 비활성 저장 버튼은 <code>btn_cm save icon disabled</code>로 지정합니다.
    순서 자체가 결과를 변경하지는 않으나, 순서를 통일하면 인수자가 조합 내용을 식별하기 용이합니다.</p>
    <div class="warn"><b>표에 포함된 <code>w2tb_adaptive_layout</code>은 자동 적용되지 않습니다.</b>
    라벨과 값을 한 쌍씩 세로로 배치하는 어댑티브 표를 사용하려면 표 레이어 이름에 해당 클래스를
    <b>직접 지정해야 합니다.</b> 누락 시 경고 없이 일반 표로 변환됩니다. 배치 변경 방식은
    <b>10장</b>에서 다룹니다.</div>
    <div class="note"><b><code>w2tb_tr</code>은 지정하지 않아도 됩니다.</b> 행 레이어에 해당 클래스를
    지정하지 않아도 변환기가 항상 추가합니다. 표 관련 클래스는 <b>직접 지정해야 적용되는 값</b>과
    <b>자동으로 추가되는 값</b>으로 구분됩니다.</div>

    <h2>3.4 자체 CSS를 쓰는 경우</h2>
    <p>템플릿 스타일을 사용하지 않고 자체 디자인 시스템의 CSS를 적용하는 경우에도 <b>클래스 이름은
    3.3의 표준 값을 그대로 사용합니다.</b> 변경 대상은 해당 클래스에 연결된 CSS의 <b>값</b>에 한정됩니다.
    이름은 규약이며, 색상·치수 등의 값만 자체 정의 대상입니다.</p>
    <div class="note">적용 방식은 신규 이름 정의가 아니라 <b>표준 이름에 자체 값을 연결하는 것</b>입니다.
    3.3 표의 클래스 이름은 유지하고 값만 자체 스타일시트로 대체합니다. <code>btn_cm</code> 대신
    <code>myBtn</code>과 같이 이름을 신규 정의하면 변환기가 해당 레이어를 규격대로 처리하지 못하므로,
    CSS를 작성해도 적용되지 않습니다.</div>
  `,
  structure: `
    <h2>4.1 레이어 중첩과 결과 구조</h2>
    <p class="lead">Figma의 레이어 중첩 순서와 깊이는 결과 XML의 태그 부모-자식 관계로 그대로
    이관됩니다. 이름(2장)으로 컴포넌트를, 클래스(3장)로 형태를 정의한 뒤에는 해당 컴포넌트를
    <b>어느 레이어 하위에 어떤 순서로</b> 배치할지가 본 장의 대상입니다.</p>
    <p>그룹 레이어 하위에 텍스트 레이어 두 개를 배치하면 결과에서도 그룹 태그 하나가 열리고
    그 하위에 텍스트 태그 두 개가 배치됩니다. 그룹을 한 겹 추가하면 결과 태그도 한 겹 추가됩니다.
    Figma 레이어 패널의 들여쓰기가 결과 XML의 들여쓰기와 일치합니다.</p>
    <p>따라서 <b>화면상 겹쳐 보이도록 배치하는 것만으로는 부모-자식 관계가 성립하지 않습니다.</b>
    레이어 패널에서 실제 하위 항목으로 포함되어 있어야 합니다. 시각적으로 특정 영역 위에 배치되어
    있더라도 레이어 패널에서 해당 영역의 하위가 아닌 위치에 있으면 변환기는 두 레이어의 관계를
    인식하지 못합니다.</p>

    <h3>화면의 기본 골격</h3>
    <p>개별 컴포넌트를 배치하기 전에 화면 전체를 포함하는 <b>그룹 하나</b>를 생성합니다.
    6~18장의 모든 컴포넌트는 이 그룹 하위에 배치됩니다.</p>
    <pre>화면 최상위 그룹        class <b>sub_contents</b>   ← 화면에 하나만
├ 화면 제목·경로        class pgtbox (7장)
├ 조회영역              class schbox (8장)
├ 섹션 제목             class titbox (7장)
├ 내용                  표 · 그리드 · 탭 …
├ 섹션 제목             class titbox
└ 내용                  …</pre>
    <ul class="pts">
      <li><b>최상위 그룹</b> — <code>scr_group_sub_contents</code>와 같이 지정해
      <code>sub_contents</code> 클래스를 적용합니다. 화면당 하나만 배치합니다.</li>
      <li><b>섹션 래퍼는 사용하지 않습니다</b> — 제목(<code>titbox</code>)과 내용을 <b>동일 레벨</b>에
      배치합니다. 제목이 나올 때마다 새 섹션이 시작되는 구조로 처리됩니다.</li>
      <li><b>화면 제목</b> — 화면 상단의 제목·경로(<code>pgtbox</code>, 7장)도 동일 레벨의
      첫 번째 하위 항목으로 배치합니다.</li>
    </ul>
    <div class="note"><code>sub_contents</code>는 <b>컴포넌트가 아니라 클래스</b>입니다.
    컴포넌트 조각은 <code>group</code>이며 <code>sub_contents</code>는 클래스 자리에 위치합니다(2.1).</div>

    <h2>4.2 내부 구조가 반영되지 않는 컴포넌트</h2>
    <div class="warn">
    <p><b>다음 컴포넌트는 내부 구조를 작성해도 결과에 반영되지 않습니다.</b> 레이어 이름이 아래
    목록과 일치하면 변환기는 해당 레이어를 외형만 있는 컨테이너로 처리하고 <b>하위 레이어를
    전부 제외합니다.</b> 실제 형태와 동작은 화면 실행 시 프레임워크가 자체적으로 생성합니다.</p>
    <ul class="pts">
      <li><code>schedulecalendar</code> · <code>floatinglayer</code> · <code>widget</code> ·
      <code>calendar</code> · <code>inputcalendar</code></li>
      <li><code>secret</code> · <code>searchbox</code> · <code>textarea</code> ·
      <code>upload</code> · <code>spinner</code></li>
      <li><code>fliptoggle</code> · <code>pagecontrol</code> · <code>progressbar</code> ·
      <code>datepicker</code> · <code>slider</code> · <code>pagelist</code></li>
    </ul>
    <p>가장 빈번한 오작업 대상은 <code>calendar</code>·<code>schedulecalendar</code>입니다. 헤더·요일·날짜
    칸을 레이어로 개별 작성해도 변환기는 내부를 참조하지 않으므로, 작업량과 무관하게 결과는
    빈 컴포넌트와 동일합니다.</p>
    <div class="note"><b>단, 빈 프레임으로 두는 것은 권장하지 않습니다.</b> 인수자가 확인하는 대상은
    결과 XML이 아니라 <b>Figma 화면</b>이며, 빈 컨테이너만 있으면 해당 위치에 배치될 컴포넌트를
    식별할 수 없습니다. <b>외형은 식별 가능한 수준까지 작성하되, 내부를 상세하게 작성하지는
    않습니다.</b> 결과에 반영되지 않는 것과 작성하지 않아도 되는 것은 별개입니다(21.5).<br>
    어디까지 그리면 되는지는 <b>컴포넌트별 챕터의 「그리는 기준선」</b>에 항목마다 적어 두었습니다 —
    달력은 머리와 요일 한 줄까지(16.2), 스케쥴 캘린더는 툴바와 주 한 줄까지(17.1), 위젯은 제목과 빈 본문까지(18.2),
    떠 있는 레이어는 제목바와 빈 본문까지(18.3), 토글·날짜선택기 같은 위젯은 겉모습 한 겹까지(15.2),
    그리드 셀 안은 아이콘 하나까지(11.2)입니다.</div>
    <p>버튼·트리거·앵커·인풋·셀렉트·멀티셀렉트·체크박스·라디오도 내부 레이어 구조를 그대로
    출력하지 않는다는 점은 동일합니다. 단 전부 제외되지는 않으며, <b>레이어에 작성된 텍스트만</b>
    추출해 사용합니다.</p>
    <ul class="pts">
      <li><b>버튼·트리거·앵커</b> — 내부 텍스트 레이어의 값을 라벨로 사용합니다. 버튼의 경우
      라벨은 속성이 아니라 <code>&lt;w2:textbox tagname="span"&gt;</code> 하위 태그로 출력됩니다.
      아이콘·배경 표현을 위한 도형 레이어는 출력되지 않으며, 해당 요소는 3장의 클래스가 담당합니다.</li>
      <li><b>인풋</b> — 텍스트 레이어의 값을 안내문구(placeholder)로만 사용하며, 그 외 하위 항목은
      제외됩니다.</li>
      <li><b>셀렉트·멀티셀렉트·체크박스·라디오</b> — 옵션으로 작성한 텍스트 값만 항목으로 추출합니다.
      옵션 표현을 위해 작성한 정렬·박스 구조는 출력되지 않으며, 변환기가 규격 형태로 재생성합니다.</li>
    </ul>
    <p>내부 구조의 출력 여부는 컴포넌트 종류에 따라 결정됩니다. <b>화면 실행 시 프레임워크가
    자체 생성하는 컴포넌트는 내부를 상세히 작성할 필요가 없습니다.</b></p>
    <div class="note"><b>내부를 참조하지 않는 컴포넌트의 <u>하위</u> 레이어는 명명 규칙을 적용하지
    않아도 됩니다.</b> 변환기가 내부를 참조하지 않기 때문이며, 라디오 하위의 <code>radio_item</code>,
    셀렉트 하위의 <code>val</code>·<code>ficon</code>과 같이 컴포넌트 목록에 없는 이름도 사용할 수
    있습니다. 명명 규칙 적용 대상은 <b>최상위 컨테이너 레이어 하나</b>입니다.
    <b>예외는 두 가지입니다</b> — 그리드 셀 하위 아이콘 이름은 컬럼 종류를 결정하며(11.2),
    트리 하위에 <code>check</code>가 포함된 레이어가 있으면 체크박스가 활성화됩니다(12.1).</div>
    </div>

    <h2>4.3 텍스트 출력 방식</h2>
    <p>일반 텍스트 레이어는 4.2의 컴포넌트와 달리 <b>텍스트 내용이 결과에 그대로 반영됩니다.</b>
    레이어에 입력한 값이 결과의 라벨이 됩니다.</p>
    <ul class="pts">
      <li><b>여러 줄</b> — Figma에서 줄바꿈(Enter)으로 구분한 줄은 결과에서도 줄바꿈으로 연결됩니다.</li>
      <li><b>불릿 목록</b> — Figma 텍스트 도구에서 목록 스타일을 <b>글머리 기호</b>로 지정한 줄은
      앞에 <code>•</code>가 자동으로 추가됩니다. 기호를 직접 입력할 필요는 없습니다.</li>
      <li><b>번호 매기기 목록</b> — 동일 도구에서 <b>번호 매기기</b> 스타일을 지정한 줄은
      <code>1.</code>·<code>2.</code> 형태의 번호가 자동으로 추가됩니다. 단, 부여되는 번호는 목록의
      순번이 아니라 <b>해당 텍스트 레이어 내의 줄 번호</b>입니다 — <b>번호 목록은 텍스트 레이어의 첫
      줄부터 작성합니다.</b> 상단에 제목 줄이 있으면 번호가 <code>2.</code>부터 시작합니다.</li>
    </ul>
    <details>
      <summary>하나의 텍스트에 복수 스타일이 적용된 경우 — 별도 조치 불필요</summary>
      <p>하나의 텍스트 레이어에서 글자마다 색상이나 굵기를 다르게 지정한 경우, 결과에서도 스타일이
      변경되는 지점마다 텍스트가 분리되어 출력됩니다. 위치와 표시 형태는 유지되므로 별도 조치는
      필요하지 않습니다. 동일 위치에 텍스트 태그가 복수로 출력되는 경우 이 규칙에 해당합니다.</p>
    </details>

    <h2>4.4 변환기 자동 생성 항목</h2>
    <div class="note">
    <p>표·그리드·브레드크럼 작성 시 다음 항목은 <b>직접 작성하지 않아도</b> 변환기가 자동으로
    생성합니다.</p>
    <ul class="pts">
      <li><b>행의 표 전용 클래스</b> — 행 레이어 이름에 클래스를 지정하지 않아도 변환기가 표 행에
      필요한 클래스를 항상 추가합니다.</li>
      <li><b>비셀 하위 항목의 셀 래핑</b> — 행 레이어 직속에 셀(th/td)이 아닌 레이어가 포함되어도
      변환기가 셀로 래핑하여 표 구조를 유지합니다.</li>
      <li><b>셀 속성 블록</b> — 셀의 헤더 여부, 병합 셀 수(가로·세로) 등의 속성 블록을 변환기가
      자동으로 생성합니다.</li>
      <li><b>표의 열 너비 정의</b> — 열 너비를 정의하는 레이어를 별도로 작성하지 않아도 변환기가
      각 셀의 측정 너비를 기준으로 자동 생성합니다.</li>
      <li><b>브레드크럼의 목록 태그</b> — 경로 표시(브레드크럼) 레이어에 목록 인식에 필요한 태그가
      자동으로 추가됩니다.</li>
      <li><b>그리드 영역 보정</b> — 그리드 영역에 그리드 레이어 없이 데이터 행만 배치된 경우,
      변환기가 그리드를 삽입하여 구조를 보정합니다.</li>
    </ul>
    <p>표·그리드·브레드크럼에서는 <b>셀 내용과 이름·클래스</b>만 작성하면 되며, 연결 구조는 변환기가
    담당합니다.</p>
    </div>

    <h2>4.5 숨김 레이어 처리</h2>
    <p>Figma에서 표시 아이콘을 해제한 <b>숨김 상태의 레이어는 결과에 출력되지 않습니다.</b>
    화면에서만 감춰지는 것이 아니라 변환기가 해당 레이어를 완전히 건너뜁니다. 상위 레이어를
    숨긴 경우에도 해당 레이어와 모든 하위 항목이 함께 제외됩니다.</p>
    <p>이 동작은 두 가지 상황에 적용됩니다. 시안 비교를 위해 여러 버전을 중첩해 두고 하나만
    표시하는 경우, 나머지 버전이 결과에 포함될 우려 없이 유지할 수 있습니다. 반면 실제로 출력되어야
    하는 레이어를 작업 중 숨김 처리한 상태로 전달하면 해당 레이어는 결과에서 제외됩니다. 이때
    오류나 빈 영역은 발생하지 않습니다.</p>
    <div class="warn">처리 방침이 확정되지 않은 숨김 레이어가 있으면 해당 영역이 결과에서 누락됩니다.
    전달 전 레이어 패널에서 확인해야 하는 항목이며, 점검 절차는 19장에서 다룹니다.</div>
  `,
  autolayout: `
    <h2>5.1 크기 값의 두 가지 전달 경로</h2>
    <p class="lead">Figma에서 지정한 치수와 색상은 <b>두 가지 경로</b>로 결과 화면에 반영됩니다 —
    <b>클래스(CSS)</b>를 경유하는 경로와 <b>XML에 직접 출력</b>되는 경로입니다. 본 장은 각 값이 어느
    경로로 전달되는지를 다룹니다.</p>
    <div class="note"><p><b>기본 경로는 CSS입니다.</b> 오토레이아웃의 방향(가로·세로), 정렬, 내부 여백,
    요소 간 간격은 <b>XML에 출력되지 않으며</b> 클래스에 연결된 CSS가 담당합니다. CSS는 디자인을 기준으로
    작성되므로 <b>Figma에서 지정한 여백·간격은 CSS를 통해 화면에 반영됩니다.</b> XML로 중복 전달할
    필요가 없습니다.</p></div>
    <p>따라서 <b>2~4장의 이름·클래스·구조가 규격에 맞다면 오토레이아웃은 작업 편의에 따라 사용할 수
    있습니다.</b> 간격 값의 차이는 XML 결과를 변경하지 않습니다.</p>
    <p><b>단, 네 가지 값은 XML에 직접 출력됩니다.</b> 직접 출력된 값은 <b>인라인 스타일이므로 클래스보다
    우선 적용</b>되며, 해당 네 가지는 <b>Figma에서 지정한 값이 최종 결과</b>가 됩니다.</p>
    <ul class="pts">
      <li><b>인풋의 폭</b> — 입력 계열 중 <b>인풋에 한정</b>되며, 한 셀에 다른 요소가 함께 있을 때만
      측정값이 출력됩니다. 그 외 필드는 종류별로 폭이 고정되어 있습니다(5.2).</li>
      <li><b><code>col_fix</code> 클래스가 적용된 고정폭 컨테이너</b> — 측정 폭이 그대로 출력됩니다.</li>
      <li><b>표의 컬럼 폭</b> — 라벨셀의 측정 폭이 해당 열의 폭으로 적용됩니다(5.3).
      단 어댑티브 표는 예외입니다.</li>
      <li><b>트리의 높이</b> — 트리 레이어의 측정 높이가 그대로 출력됩니다. 클래스가 적용된 경우에도
      출력되는 값이므로 트리의 표시 높이는 Figma에서 지정합니다(12장).</li>
    </ul>
    <p>위 네 가지 외에 스피너 높이와 같이 <b>렌더링 보정을 위해 측정값이 함께 출력되는 경우</b>가
    있습니다. 디자이너가 관리해야 하는 대상은 위 네 가지입니다.</p>
    <p><b>표와 그리드는 클래스가 적용되어 있어도 값이 함께 출력됩니다.</b> 표의 폭은 항상
    <code>width:100%</code>로 출력되며(5.3), 그리드의 컬럼 폭·높이는 변환기가 지정한 값이 적용됩니다
    (<b>11장</b> 참조).</p>
    <p><b>클래스가 없는 레이어</b>는 3장에서 다룬 대로 측정값 전체가 인라인으로 출력됩니다 —
    방향·정렬·여백·간격이 모두 포함됩니다. 이렇게 출력된 값은 CSS로 수정할 수 없습니다. 결론은 3장과
    동일합니다 — <b>클래스를 비워 두지 않습니다.</b></p>

    <h2>5.2 폼 필드의 폭</h2>
    <p class="lead">입력 계열 컴포넌트의 폭은 <b>필드 종류별로 고정되어 있습니다.</b>
    Figma에서 지정한 폭이 결과에 반영되는 대상은 <b>인풋에 한정</b>되며, 여기에도 조건이 적용됩니다.</p>
    <ul class="pts">
      <li><b>인풋</b> — 해당 셀에 매핑되는 다른 요소가 <b>없으면</b> <code>width:100%</code>,
      <b>있으면</b> Figma 측정 폭이 그대로 출력됩니다. 여기서 다른 요소에는
      <code>~</code>·<code>@</code> 등의 <b>구분자 텍스트와 버튼도 포함</b>되며, 필드만 계산하지 않습니다.</li>
      <li><b>날짜 입력칸</b> — 항상 <code>width:120px</code>이며 작성 폭과 무관합니다.</li>
      <li><b>셀렉트 · 비밀번호 · 조회상자 · 숫자 증감 · 파일 업로드</b> — 항상 <code>width:100%</code>입니다.</li>
      <li><b>여러 줄 입력칸</b> — 항상 <code>width:100%</code>이며 높이 50px이 함께 적용됩니다.</li>
      <li><b>다중선택 · 체크박스 · 라디오 · 자동완성 · 체크 콤보</b> — 폭이 출력되지 않으며
      <b>클래스(CSS)가 정의합니다.</b></li>
    </ul>
    <div class="warn"><b>폼 필드에서 Figma의 채움(Fill)·고정(Fixed) 설정은 결과에 영향을 주지 않습니다.</b>
    설정과 무관하게 위 규칙이 적용되며, 인풋에 출력되는 측정 폭도 사이징 설정과 무관하게
    <b>작성된 폭</b>이 사용됩니다. 사이징 설정이 결과에 반영되는 대상은 <code>col_fix</code> 컨테이너뿐입니다(6.2).</div>
    <div class="note"><b>필드별로 다른 폭이 필요한 경우 한 셀에 복수 요소를 배치하는 구조를 사용합니다</b>(15.3).
    이때에도 폭이 변경되는 대상은 인풋뿐이며, 그 외 필드는 Figma에서 폭을 조정할 수 없습니다.
    종류별 전체 목록은 <b>21장 부록</b>에 있습니다.</div>

    <h2>5.3 표의 컬럼 폭</h2>
    <div class="warn"><p><b>표 자체의 폭은 항상 <code>width:100%</code>로 나갑니다.</b>
    표 레이어를 크게 그리든 좁게 그리든 결과는 같으니, <b>표에서 조절할 수 있는 것은
    열 폭(=라벨셀 폭)뿐</b>입니다.</p></div>
    <p>표에서 조정 가능한 값은 <b>컬럼(열)의 폭</b>입니다. 변환기는 표 내에서 <b>셀 수가 가장 많은 행</b>
    (셀 수가 동일한 경우 입력 셀이 많은 행)을 기준 행으로 선택하고, 해당 행의
    <b>라벨셀(th) 측정 폭을 해당 열의 폭으로</b> 적용합니다.</p>
    <ul class="pts">
      <li><b>라벨셀(th)</b> — Figma 측정 폭이 컬럼 폭으로 적용됩니다.</li>
      <li><b>입력셀(td)</b> — 폭 값이 출력되지 않고 비어 있는 상태로 처리되어 잔여 공간을 자동 분배합니다.
      따라서 열 폭 조정은 입력셀이 아니라 라벨셀에서 수행합니다.</li>
    </ul>
    <p>표의 열 폭은 <b>라벨셀(th)의 폭으로 조정</b>합니다. 기준 행은 변환기가 자동 선택하므로 행마다
    라벨 폭이 다르면 어느 행이 기준이 될지 예측하기 어렵습니다. 동일 열의 라벨셀은 모든 행에서 폭을
    통일해 작성합니다.</p>
    <div class="warn"><b>어댑티브 표(<code>w2tb_adaptive_layout</code>)는 예외입니다.</b>
    어댑티브 표에서는 라벨열이 <b>항상 150px로 고정</b>되며 폭을 조정해도 변경되지 않습니다.
    어댑티브 표를 사용하는 경우 라벨셀 폭 조정은 불필요합니다(10장 참조).</div>
    <div class="note">행과 셀의 자동 연결 방식은 4장에서 다룹니다. 본 장에서는 생성된 표 구조에서
    <b>어떤 셀의 폭이 결과에 반영되는지</b>만 다룹니다.</div>

    <h2>5.4 오해가 발생하기 쉬운 항목</h2>
    <p>오토레이아웃과 크기에 관해 오해가 발생하기 쉬운 항목을 정리했습니다.</p>
    <div class="tblwrap">
      <table>
        <thead>
          <tr><th>예상 동작</th><th>실제 동작</th></tr>
        </thead>
        <tbody>
          <tr><td>정렬을 가운데로 지정하면 결과도 가운데 정렬된다</td>
          <td>정렬은 클래스(CSS)가 정의하며 XML에 출력되지 않습니다.</td></tr>
          <tr><td>간격을 넓게 지정하면 결과도 동일하게 적용된다</td>
          <td>간격도 클래스(CSS)가 정의하며, Figma에서 지정한 간격은 CSS에 반영됩니다.</td></tr>
          <tr><td>표를 넓게 작성하면 해당 폭이 유지된다</td>
          <td>표의 폭은 항상 <code>width:100%</code>로 출력됩니다(5.3).</td></tr>
          <tr><td>입력셀(td)의 폭을 지정하면 해당 컬럼 폭이 된다</td>
          <td>컬럼 폭은 라벨셀(th)이 결정합니다(5.3).</td></tr>
          <tr><td>필드 폭을 지정하면 해당 폭으로 출력된다</td>
          <td>필드 폭은 종류별로 고정되어 있습니다(5.2). 측정 폭이 출력되는 대상은 한 셀에 다른 요소와
          함께 배치된 <b>인풋</b>뿐입니다.</td></tr>
          <tr><td>필드를 채움(Fill)으로 지정하면 셀을 채우고, 고정(Fixed)으로 지정하면 해당 폭이 유지된다</td>
          <td>폼 필드에서 채움·고정 설정은 결과에 영향을 주지 않습니다(5.2).</td></tr>
          <tr><td>고정폭 컨테이너 형태로 작성하면 폭이 유지된다</td>
          <td>고정폭이 출력되려면 이름에 <code>col_fix</code> 클래스를 지정해야 합니다. 미지정 시
          해당 폭은 클래스(가변폭)가 결정합니다.</td></tr>
        </tbody>
      </table>
    </div>
    <p>본 장의 요약은 다음과 같습니다. <b>Figma 측정값이 XML에 출력되는 대상은 조합형 셀의 인풋 폭,
    <code>col_fix</code> 컨테이너, 표의 라벨셀 폭, 트리 높이 네 가지이며</b> 그 외에는 클래스(CSS) 또는
    변환기가 지정한 값이 적용됩니다. <b>해당 네 가지만 정확히 작성하고</b> 그 외 배치는 작업 편의에
    따릅니다.</p>
  `,
  checklist: `
    <p class="lead">전달 전 확인 목록입니다. 파트 II의 네 가지 항목 순서를 따르며,
    19.5에는 <b>Figma 캔버스에서 확인할 수 없어 변환 결과로 확인해야 하는 항목</b>을 정리했습니다.</p>

    <h2>19.1 이름</h2>
    <ul class="pts">
      <li>매핑 대상 레이어가 <code>접두어_컴포넌트_클래스</code> 형식으로 작성되었는지 확인합니다(2.1)</li>
      <li>컴포넌트 조각이 컴포넌트 목록(부록 21.1)의 값과 <b>완전히 일치</b>하는지 확인합니다 — 대소문자는 구분하지 않으나 철자는 일치해야 합니다(2.2)</li>
      <li>단일 단어로 작성된 이름이 없는지 확인합니다 — <code>th</code>·<code>td</code>에도 접두어를 지정합니다(2.2)</li>
      <li>장식 레이어 이름에 컴포넌트 키워드가 포함되지 않았는지 확인합니다 — <code>context_area</code>의 <code>text</code>가 해당합니다(2.4)</li>
      <li>상태가 이름에 명시되었는지 확인합니다 — <code>disabled</code>·<code>readonly</code>·<code>req</code>·<code>error</code>. 색상만으로 표현한 상태는 전달되지 않습니다(15.1)</li>
      <li>상태 단어가 다른 문자와 결합되지 않았는지 확인합니다 — <code>disabled2</code>는 조각 단위 완전일치에 해당하지 않습니다(15.1)</li>
      <li>그리드·트리·아코디언의 <b>구조 이름</b>이 규격에 맞는지 확인합니다 — 행에 <code>header</code>/<code>row</code>/<code>footer</code>(11.1),
      트리 노드는 두 번째 조각이 <code>node</code>와 완전 일치(12.1), 아코디언은 <code>panels</code>·<code>paneltitle</code>·<code>panelcontent</code>(14.1)</li>
      <li>위젯 이름이 <code>w2widget</code>으로 <b>종료</b>되는지 확인합니다 — 뒤에 다른 값이 추가되면 위젯으로 인식되지 않습니다(18.2)</li>
    </ul>

    <h2>19.2 클래스</h2>
    <ul class="pts">
      <li>클래스가 부록 21.2의 표준 값이거나 컴포넌트별 챕터에서 안내한 값인지 확인합니다(3.3)</li>
      <li>형태가 클래스로 결정되는 레이어(컨테이너·버튼·제목·필드 등)에 클래스가 지정되었는지 확인합니다 —
      미지정 시 측정값이 인라인으로 출력되어 CSS로 수정할 수 없습니다(3.2)</li>
      <li>단 <b>표·행·라벨셀·값셀과 진행 단계는 클래스를 지정하지 않아도 됩니다</b> —
      변환기가 <code>w2tb</code>·<code>w2tb_tr</code>·<code>w2tb_th</code>·<code>w2tb_td</code>·<code>processbar</code>를 추가합니다(3.2)</li>
      <li>어댑티브 표의 경우 표 레이어 이름에 <code>w2tb_adaptive_layout</code>이 지정되었는지 확인합니다 — 자동 추가되지 않습니다(3.3 · 10.2)</li>
      <li>목록에 <code>listbox</code> 클래스가 지정되었는지 확인합니다 — 미지정 시 불릿과 줄 간격이 적용되지 않습니다(18.1)</li>
      <li>버튼 클래스 조합 순서가 규격에 맞는지 확인합니다 — <code>btn_cm</code> → 변형 → <code>icon</code> → <code>disabled</code>(3.3)</li>
      <li>아이콘 버튼에 <code>icon</code> 토큰이 지정되었는지 확인합니다 — 미지정 시 아이콘과 텍스트가 모두 없는 버튼이 됩니다(13.1)</li>
      <li>패널 비율이 필요한 위치에 <code>col_2</code>~<code>col_9</code>가, 고정폭이 필요한 위치에 <code>col_fix</code>가 지정되었는지 확인합니다(6.1 · 6.2)</li>
    </ul>

    <h2>19.3 구조</h2>
    <ul class="pts">
      <li>처리 방침이 미확정된 숨김 레이어가 없는지 확인합니다 — 해당 영역은 결과에서 누락됩니다(4.5)</li>
      <li>시각적으로만 겹쳐 배치되고 실제 하위 항목이 아닌 레이어가 없는지 확인합니다(4.1)</li>
      <li>내부 구조가 반영되지 않는 컴포넌트(4.2 · 16~18장)에 과도한 작업이 이루어지지 않았는지 확인합니다 — 달력·스케쥴 캘린더·떠 있는 레이어·위젯 내부가 해당합니다</li>
      <li>탭 위치 변형(9.4~9.6)에 <code>tabhost</code> 래퍼가 있는지, 어댑티브 탭(9.7)에는 없는지 확인합니다</li>
      <li>트리 계층은 <b>중첩</b>으로, 그리드 드릴다운 계층은 <b>들여쓰기</b>로 작성되었는지 확인합니다 — 두 방식은 서로 반대입니다(11.4 · 12.1)</li>
      <li>아코디언 본문에 표나 복수 문단이 포함되지 않았는지 확인합니다 — 첫 텍스트 한 줄만 전달됩니다(14.1)</li>
      <li>위젯의 레이어 순서가 화면 표시 순서와 일치하는지 확인합니다 — 배치는 레이어 순서로 재계산됩니다(18.2)</li>
    </ul>

    <h2>19.4 크기</h2>
    <ul class="pts">
      <li>측정값이 출력되는 네 가지가 의도한 값으로 작성되었는지 확인합니다 — 조합형 셀의 인풋 폭 · <code>col_fix</code> 폭 · 표의 라벨셀 폭 · 트리 높이(5.1)</li>
      <li>동일 열의 라벨셀 폭이 모든 행에서 일치하는지 확인합니다 — 기준 행은 변환기가 선택합니다(5.3)</li>
      <li>필드 폭 조정에 불필요한 작업이 이루어지지 않았는지 확인합니다 — 측정 폭이 출력되는 대상은 다른 요소와 같은 셀에 있는 인풋뿐입니다(5.2)</li>
      <li>그리드 셀·행의 치수 조정에 불필요한 작업이 이루어지지 않았는지 확인합니다 — 컬럼 폭 70px, 높이 153px는 변환기가 지정합니다(11.1)</li>
    </ul>

    <h2>19.5 변환 결과로만 확인 가능한 항목</h2>
    <div class="warn"><b>아래 항목은 Figma 캔버스에서 식별할 수 없습니다.</b>
    변환 결과 또는 실제 화면에서 확인해야 합니다. 자동매핑 모달(2.6)에서 판정 결과를 먼저 검토한 뒤
    변환 결과에서 아래 항목을 확인합니다.</div>
    <ul class="pts">
      <li><b>어댑티브 표</b> — 표 태그의 <code>class</code>에 <code>w2tb_adaptive_layout</code>이 포함되었는지 확인합니다. 일반 표와 Figma상 표시가 동일합니다(8.2 · 10.2)</li>
      <li><b>스크롤 탭</b> — 상위 레이어 이름의 첫 조각이 <code>scroll</code>인지 확인합니다. 기본탭과 캔버스에서 구분되지 않습니다(9.3)</li>
      <li><b>읽기전용</b> — 셀렉트·멀티셀렉트·체크박스·라디오·업로드는 결과에 출력되지 않습니다. 해당 다섯 가지는 비활성으로만 처리됩니다(15.1)</li>
      <li><b>폴백 오매핑</b> — 의도와 다르게 판정된 레이어가 없는지 확인합니다. 자동매핑 모달에서 확인 가능합니다(2.3 · 2.6)</li>
      <li><b>데이터 행 누락</b> — 그리드 데이터가 비어 있으면 행 이름에 <code>row</code>가 포함되었는지 확인합니다(11.1)</li>
      <li><b>의도하지 않은 체크박스</b> — 트리에 체크박스가 활성화된 경우 이름에 <code>check</code>가 포함된 레이어가 있는지 확인합니다(12.1)</li>
    </ul>
  `,
  symptoms: `
    <p class="lead">변환 결과가 의도와 다를 때 원인을 식별하기 위한 표입니다. <b>증상 → 원인 → 참조</b>
    순서로 구성되어 있으며, 대부분의 원인은 이름의 한 조각 또는 클래스의 한 토큰에 있습니다.</p>

    <h2>20.1 다른 컴포넌트로 변환된 경우</h2>
    <div class="tblwrap">
      <table>
        <thead><tr><th>증상</th><th>원인</th><th>참조</th></tr></thead>
        <tbody>
          <tr><td>버튼이 날짜 입력칸으로 변환됨</td>
          <td>컴포넌트 조각이 목록과 불일치하여 변환기가 이름 전체에서 인식 가능한 단어로 추정</td><td>2.3 · 2.4</td></tr>
          <tr><td>설명 영역이 텍스트박스로 변환됨</td>
          <td>이름에 <code>text</code>가 포함됨(<code>context_area</code>)</td><td>2.4</td></tr>
          <tr><td>그리드의 특정 컬럼이 셀렉트로 변환됨</td>
          <td>컬럼 제목에 <code>select</code> 등 영문 위젯 이름을 사용</td><td>11.2</td></tr>
          <tr><td>셀렉트 두 종류가 동일한 형태로 출력됨</td>
          <td>이름에 <code>selectnative</code>·<code>selectsel</code>이 없어 기본 셀렉트로 변환</td><td>15.1</td></tr>
        </tbody>
      </table>
    </div>

    <h2>20.2 작성 항목이 결과에 없는 경우</h2>
    <div class="tblwrap">
      <table>
        <thead><tr><th>증상</th><th>원인</th><th>참조</th></tr></thead>
        <tbody>
          <tr><td>레이어 전체가 누락됨</td><td>숨김 상태로 작성됨</td><td>4.5</td></tr>
          <tr><td>달력·스케쥴 캘린더 내부가 비어 있음</td>
          <td>내부를 참조하지 않는 컴포넌트이며, 실제 형태는 프레임워크가 생성</td><td>4.2 · 16.2 · 17.1</td></tr>
          <tr><td>그리드 데이터가 비어 있음</td><td>데이터 행 이름에 <code>row</code>가 없어 행으로 인식되지 않음</td><td>11.1</td></tr>
          <tr><td>드릴다운 행이 누락됨</td><td>행을 중첩 구조로 작성함. 계층은 들여쓰기로 표현</td><td>11.4</td></tr>
          <tr><td>트리 노드가 하위 항목과 함께 누락됨</td><td>두 번째 조각이 <code>node</code>와 완전 일치하지 않음</td><td>12.1</td></tr>
          <tr><td>위젯이 화면에 표시되지 않음</td><td>이름이 <code>w2widget</code>으로 종료되지 않음</td><td>18.2</td></tr>
          <tr><td>아코디언 본문의 표·버튼이 누락됨</td><td>본문은 첫 텍스트 한 줄만 전달됨</td><td>14.1</td></tr>
          <tr><td>브레드크럼 구분자(<code>/</code>)가 누락됨</td><td>구분자는 CSS가 생성하며 작성한 텍스트는 매핑되지 않음</td><td>7.1</td></tr>
        </tbody>
      </table>
    </div>

    <h2>20.3 형태가 의도와 다른 경우</h2>
    <div class="tblwrap">
      <table>
        <thead><tr><th>증상</th><th>원인</th><th>참조</th></tr></thead>
        <tbody>
          <tr><td>스타일이 적용되지 않음</td>
          <td>클래스 미지정으로 측정값이 인라인 출력됨. 인라인은 CSS보다 우선 적용</td><td>3.2 · 5.1</td></tr>
          <tr><td>클래스를 지정했으나 CSS가 적용되지 않음</td><td>표준 이름이 아닌 값을 신규 정의함</td><td>3.3 · 3.4</td></tr>
          <tr><td>Figma에서 지정한 버튼 색상이 반영되지 않음</td>
          <td>버튼 색상은 클래스가 결정하며, 채움 형태는 <code>fill</code> 토큰으로 지정</td><td>13.1</td></tr>
          <tr><td>비활성 트리거가 활성과 동일한 색상으로 표시됨</td>
          <td>트리거는 Figma 채움색이 인라인으로 출력되어 CSS보다 우선 적용됨. 상태별로 색상을 구분해 지정</td><td>13.2</td></tr>
          <tr><td>아이콘이 표시되지 않음</td><td>클래스에 변형 토큰이 없음. 아이콘은 클래스가 결정</td><td>13.1</td></tr>
          <tr><td>아이콘 버튼이 빈 상태로 출력됨</td><td><code>icon</code> 토큰이 없어 라벨과 아이콘이 모두 없는 버튼으로 변환</td><td>13.1</td></tr>
          <tr><td>목록 불릿이 누락되어 단일 블록으로 표시됨</td><td><code>listbox</code> 클래스가 없어 <code>ul</code>/<code>li</code>가 아닌 그룹으로 변환</td><td>18.1</td></tr>
          <tr><td>제목 크기가 h3·h4 구분 없이 동일함</td><td>클래스가 <code>tit_main</code>/<code>tit_sub</code>로 구분되지 않음</td><td>7.2 · 7.3</td></tr>
          <tr><td>진행 단계에 완료·현재 상태 표시가 없음</td><td>상태를 색상으로만 표현함. 세 번째 조각에 <code>finish</code>·<code>on</code>을 지정</td><td>18.4</td></tr>
        </tbody>
      </table>
    </div>

    <h2>20.4 배치가 의도와 다른 경우</h2>
    <div class="tblwrap">
      <table>
        <thead><tr><th>증상</th><th>원인</th><th>참조</th></tr></thead>
        <tbody>
          <tr><td>가로 배치한 영역이 세로로 배치됨</td><td><code>lybox</code> 미지정. 오토레이아웃 방향은 XML에 출력되지 않음</td><td>6.1</td></tr>
          <tr><td>패널 폭 비율이 적용되지 않고 균등 분할됨</td><td>비율 클래스(<code>col_2</code>~<code>col_9</code>) 미지정</td><td>6.1</td></tr>
          <tr><td>표가 라벨·값 한 쌍씩 세로로 배치됨</td><td>어댑티브 표에 해당하며, 화면 폭과 무관하게 항상 적용됨</td><td>10.2</td></tr>
          <tr><td>표 컬럼 폭이 의도와 다름</td><td>라벨셀(th)의 측정 폭이 컬럼 폭으로 적용됨. 행마다 다르면 기준 행 예측이 어려움</td><td>5.3</td></tr>
          <tr><td>어댑티브 표에서 라벨열 폭이 변경되지 않음</td><td>어댑티브 표의 라벨열은 150px로 고정됨</td><td>10.2</td></tr>
          <tr><td>필드가 셀을 채워 폭 조정이 되지 않음</td><td>필드 폭은 종류별로 고정되어 있으며 대부분 <code>width:100%</code></td><td>5.2</td></tr>
          <tr><td>날짜 입력칸만 좁게 출력됨</td><td>날짜 입력칸은 항상 120px이며 Figma 작성 폭과 무관함</td><td>5.2</td></tr>
          <tr><td>탭이 왼쪽이 아닌 상단에 배치됨</td><td><code>tabhost</code> 래퍼가 없어 어댑티브로 판정됨. 어댑티브는 탭 위치를 적용하지 않음</td><td>9.4 · 9.7</td></tr>
          <tr><td>탭이 아코디언 형태로 표시됨</td><td>어댑티브 탭에 해당하며, 기준 폭 1920px 미만에서 해당 형태로 렌더링됨</td><td>9.7</td></tr>
          <tr><td>트리 계층이 모두 1단계로 표시됨</td><td>들여쓰기로 계층을 표현함. 트리는 중첩 구조만 참조</td><td>12.1</td></tr>
          <tr><td>드릴다운 단계가 구분되지 않음</td><td>들여쓰기 차이가 4px 이하임. 단계당 16px씩 일정하게 지정</td><td>11.4</td></tr>
          <tr><td>위젯 순서가 캡처와 다름</td><td>배치는 캔버스 위치가 아니라 레이어 순서로 재계산됨</td><td>18.2</td></tr>
        </tbody>
      </table>
    </div>

    <h2>20.5 전달 대상이 아닌 항목</h2>
    <p>아래 항목은 작성 오류가 아니라 <b>Figma로 전달할 수 있는 수단이 없는 대상</b>입니다. 필요한 경우
    개발 단계에서 적용합니다.</p>
    <div class="tblwrap">
      <table>
        <thead><tr><th>기대 항목</th><th>실제 동작</th><th>참조</th></tr></thead>
        <tbody>
          <tr><td>컬럼 정렬·툴팁·사용자필터</td><td>해당 기능을 활성화하는 속성이 결과에 포함되지 않음</td><td>11.3</td></tr>
          <tr><td>탭·패널·행의 선택 및 펼침 상태</td><td>초기 상태 전달 수단이 없음</td><td>9.1 · 14.1</td></tr>
          <tr><td>셀렉트·라디오의 선택 항목</td><td>항목 텍스트만 수집되며 선택 상태는 전달되지 않음</td><td>8.1 · 15.1</td></tr>
          <tr><td>그리드 셀 셀렉트의 선택 항목 내용</td><td><code>new row</code> 3개로 고정됨</td><td>11.2</td></tr>
          <tr><td>읽기전용 셀렉트·체크박스·라디오·업로드</td><td>해당 다섯 가지는 비활성으로만 처리됨</td><td>15.1</td></tr>
          <tr><td>진행바 진행률·페이지 목록의 현재 페이지</td><td>값은 화면 실행 후 데이터가 결정</td><td>15.2 · 11.5</td></tr>
          <tr><td>안내 툴팁(물음표 표시)</td><td>전달 수단이 없음</td><td>7.2 · 18.1</td></tr>
          <tr><td>대용량 목록(virtual) 처리</td><td>클래스 이름만 전달되며 동작은 활성화되지 않음</td><td>12.2</td></tr>
          <tr><td>모바일 화면의 브레드크럼</td><td>모바일에서는 규칙상 제외됨</td><td>7.1</td></tr>
        </tbody>
      </table>
    </div>
  `,
  appendix: `
    <p class="lead">참조용 장입니다. 본문에서 「부록 21.x 참조」로 지시한 표를 수록합니다.</p>

    <h2>21.1 컴포넌트 이름 전체표</h2>
    <p>레이어 이름의 <b>컴포넌트 조각</b>에 사용하는 값입니다(2.2). 대소문자는 구분하지 않으나 철자는
    일치해야 합니다.</p>
    {{REGISTRY_NAMES}}
    <div class="note">표에 없는 값을 컴포넌트 조각에 사용하면 변환기가 <b>이름 전체에서 인식 가능한 단어를
    탐색해 추정</b>합니다(2.3). 이 과정에서 별도 경고 없이 오매핑이 발생하므로, 21.3의 단어를 제외하는 것과
    본 표의 값을 사용하는 것을 함께 적용합니다.</div>

    <h2>21.2 표준 클래스 전체표</h2>
    <p>레이어 이름의 <b>세 번째 조각 이후</b>에 사용하는 값입니다(3.1). 복수인 경우 공백으로 구분합니다.</p>
    {{CLASS_TABLE}}
    <div class="note">본 표는 <b>디자인 템플릿에서 실제 사용 중인</b> 클래스 목록입니다.
    컴포넌트별 챕터(6~18장)에서 안내하는 클래스는 표에 없어도 사용할 수 있으며, 레이아웃 비율을 지정하는
    <code>col_2</code>~<code>col_9</code>가 이에 해당합니다(6.1).</div>

    <h2>21.3 이름에 사용을 제외해야 하는 단어</h2>
    <p>변환기가 인식하는 <b>영문 단어 전체</b>입니다. 컴포넌트 조각이 21.1과 불일치하는 경우, 변환기는
    이름 전체에서 아래 단어를 탐색해 컴포넌트를 추정합니다. <b>접두어와 클래스 자리에서 해당 단어의 사용을
    제외합니다</b>(2.4).</p>
    <div class="note"><code>테이블</code>·<code>선택</code>·<code>버튼</code> 등 <b>한글 위젯 이름도 동일한 방식으로
    매칭됩니다.</b> 단 해당 폴백은 <b>컴포넌트 조각이 불일치할 때만</b> 동작하므로, 레이어 이름을 2.1의 형식으로
    작성하고 컴포넌트 조각을 21.1의 값으로 지정하면 해당 상황은 발생하지 않습니다.</div>
    <div class="warn"><b>고위험 — 길이가 짧고 사용 빈도가 높아 중복 가능성이 큰 값</b><br>
    <code>text</code> · <code>tab</code> · <code>grid</code> · <code>tree</code> · <code>node</code> ·
    <code>input</code> · <code>select</code> · <code>group</code> · <code>table</code> · <code>image</code> ·
    <code>img</code> · <code>radio</code> · <code>acd</code> · <code>td</code> · <code>th</code> · <code>tr</code></div>
    <div class="note"><b><code>tr</code>·<code>th</code>·<code>td</code>는 예외 처리 대상입니다</b> — 길이가 짧아
    <b>이름 조각과 완전히 일치할 때만</b> 매칭되도록 별도 처리되어 있습니다. 따라서 <code>star</code>·<code>track</code>과
    같이 <code>tr</code>이 포함된 이름은 매칭되지 않습니다.</div>
    <div class="tblwrap">
      <table class="clstbl">
        <tr><th>구분</th><th>단어</th></tr>
        <tr><th scope="row">중위험(영문)</th><td><code>anchor</code> · <code>button</code> · <code>calendar</code> ·
        <code>checkbox</code> · <code>secret</code> · <code>slider</code> · <code>spinner</code> ·
        <code>trigger</code> · <code>textarea</code> · <code>textbox</code> · <code>inputbox</code> ·
        <code>gridview</code> · <code>treeview</code> · <code>searchbox</code> · <code>multiselect</code> ·
        <code>autocomplete</code> · <code>checkcombobox</code> · <code>accordion</code> · <code>panels</code> ·
        <code>paneltitle</code> · <code>panelcontent</code> · <code>tabcontrol</code> · <code>pagelist</code> ·
        <code>pagecontrol</code> · <code>datepicker</code> · <code>fliptoggle</code> · <code>fileupload</code> ·
        <code>inputcalendar</code> · <code>w2calendar</code> · <code>progressbar</code> · <code>processbar</code> ·
        <code>pr_steps</code> · <code>schedulecalendar</code> · <code>floatinglayer</code> · <code>widgetcontainer</code></td></tr>
        </table>
    </div>

    <h2>21.4 폼 필드 종류 전체표</h2>
    <p>5.2와 15.1에서 참조하는 표입니다. <b>입력 계열</b>은 아래 열세 가지를 의미합니다.</p>
    <div class="tblwrap">
      <table>
        <thead><tr><th>필드</th><th>컴포넌트 조각</th><th>결과에 출력되는 폭</th><th>읽기전용</th></tr></thead>
        <tbody>
          <tr><td>입력칸</td><td><code>input</code></td><td><b>셀에 단독 배치 시 100%, 다른 요소와 함께 배치 시 측정 폭</b></td><td>지원</td></tr>
          <tr><td>비밀번호</td><td><code>secret</code></td><td>100% 고정</td><td>지원</td></tr>
          <tr><td>여러 줄 입력칸</td><td><code>textarea</code></td><td>100% 고정 + 높이 50px</td><td>지원</td></tr>
          <tr><td>셀렉트박스</td><td><code>select</code></td><td>100% 고정(<code>selectnative</code>는 폭 미출력)</td><td><b>미지원</b></td></tr>
          <tr><td>다중선택</td><td><code>multiselect</code></td><td>클래스(CSS)</td><td><b>미지원</b></td></tr>
          <tr><td>체크박스</td><td><code>checkbox</code></td><td>클래스(CSS)</td><td><b>미지원</b></td></tr>
          <tr><td>라디오</td><td><code>radio</code></td><td>클래스(CSS)</td><td><b>미지원</b></td></tr>
          <tr><td>파일 업로드</td><td><code>upload</code></td><td>100% 고정</td><td><b>미지원</b></td></tr>
          <tr><td>조회상자</td><td><code>searchbox</code></td><td>100% 고정</td><td>지원</td></tr>
          <tr><td>날짜 입력칸</td><td><code>inputcalendar</code></td><td><b>120px 고정</b></td><td>지원</td></tr>
          <tr><td>숫자 증감</td><td><code>spinner</code></td><td>100% 고정 + 측정 높이</td><td>지원</td></tr>
          <tr><td>자동완성</td><td><code>autocomplete</code></td><td>클래스(CSS)</td><td>지원</td></tr>
          <tr><td>체크 콤보</td><td><code>checkcombobox</code></td><td>클래스(CSS)</td><td>지원</td></tr>
        </tbody>
      </table>
    </div>
    <ul class="pts">
      <li><b>폭</b> — Figma 측정 폭이 결과에 반영되는 대상은 <b>입력칸(인풋) 한 종류</b>이며, 동일 셀에
      다른 매핑 요소(구분자 텍스트·버튼 포함)가 있는 경우에 한정됩니다(5.2). <b>채움(Fill)·고정(Fixed) 설정은
      모든 필드에서 결과에 영향을 주지 않습니다.</b></li>
      <li><b>읽기전용</b> — 「미지원」인 다섯 가지는 이름에 <code>readonly</code>를 지정해도 결과에 출력되지
      않으며, <code>disabled</code>로만 처리됩니다(15.1).</li>
      <li><b>상태 네 가지의 출력 위치</b> — <code>disabled</code>·<code>readonly</code>는 <b>속성</b>으로,
      <code>req</code>·<code>error</code>는 <b>클래스</b>로 출력됩니다.</li>
      <li><b>필드에 지정한 클래스</b>는 결과에 출력되지 않습니다(<code>req</code>·<code>error</code> 제외).</li>
    </ul>

    <h2>21.5 아이콘</h2>
    <p>아이콘은 <b>Figma에서 작성하는 대상</b>과 <b>결과 화면에 표시되는 대상</b>이 서로 다른 경로로
    처리됩니다. 이 구분이 명확하지 않으면 클래스가 생성하므로 작성이 불필요하다는 오해가 발생합니다.</p>
    <div class="tblwrap">
      <table>
        <thead><tr><th>대상</th><th>결정 주체</th><th>디자이너 작업</th></tr></thead>
        <tbody>
          <tr><td>Figma 화면에 표시되는 아이콘</td><td>작성한 도형·SVG</td>
          <td><b>작성합니다.</b> 인수자가 확인하는 대상이 이 화면입니다</td></tr>
          <tr><td>결과 화면에 표시되는 아이콘</td><td>클래스에 연결된 CSS</td>
          <td>클래스에 변형 토큰을 지정합니다(<code>btn_cm search</code> 등)</td></tr>
          <tr><td>아이콘 <b>파일</b> 이름</td><td>변환기가 참조하지 않음</td>
          <td><b>제한 없습니다.</b> 자체 아이콘 파일을 사용해도 변환에 영향이 없습니다</td></tr>
        </tbody>
      </table>
    </div>
    <div class="note"><b>작성한 아이콘 레이어는 결과 XML에 출력되지 않습니다.</b> 그럼에도 작성하는 이유는
    Figma 화면이 인수 대상이며 개발자·기획자가 해당 화면을 기준으로 확인하기 때문입니다.
    <b>결과에 출력되지 않는 것과 작성하지 않아도 되는 것은 별개입니다.</b></div>

    <h3>아이콘 레이어 이름이 결과에 영향을 주는 두 항목</h3>
    <p>아이콘 레이어 이름은 원칙적으로 제한이 없습니다. <b>다음 두 가지가 예외입니다.</b></p>
    <div class="warn">
      <p><b>① 그리드 셀 내부 아이콘 — 컬럼 종류를 결정합니다(11.2)</b><br>
      데이터 셀 내부 레이어 이름에 아래 조각이 <b>포함되면</b> 해당 컬럼의 종류로 적용됩니다.
      이름만 참조하므로 <b>사용한 아이콘 파일은 결과에 영향을 주지 않습니다.</b></p>
      <div class="tblwrap">
        <table class="clstbl">
          <tr><th>이름에 포함할 조각</th><th>컬럼 종류</th></tr>
          <tr><th scope="row"><code>ico_check</code></th><td>체크박스</td></tr>
          <tr><th scope="row"><code>ico_radio</code></th><td>라디오</td></tr>
          <tr><th scope="row"><code>ico_select</code></th><td>셀렉트</td></tr>
          <tr><th scope="row"><code>ico_calendar</code></th><td>달력</td></tr>
          <tr><th scope="row"><code>ico_spinner</code></th><td>숫자 증감</td></tr>
          <tr><th scope="row"><code>ico_search</code></th><td>검색 이미지</td></tr>
          <tr><th scope="row"><code>ico_drilldown</code></th><td>계층(드릴다운)</td></tr>
        </table>
      </div>
      <p><b>② 트리 내부의 <code>check</code> — 체크박스를 활성화합니다(12.1)</b><br>
      트리 <b>내부 어느 위치에든</b> 이름에 <code>check</code>가 포함된 레이어가 하나라도 있으면 트리 전체에
      체크박스가 활성화됩니다. 아이콘을 <code>ico_check</code>로 지정한 경우가 이에 해당합니다.</p>
    </div>

    <h3>자체 아이콘 사용 시</h3>
    <ul class="pts">
      <li><b>Figma</b> — 자체 아이콘 파일을 그대로 사용합니다. 변환기는 파일을 참조하지 않으며,
      위 두 예외에 해당하는 위치에서만 <b>레이어 이름</b>을 규격에 맞춥니다.</li>
      <li><b>결과 화면</b> — 실제 표시되는 아이콘은 프로젝트 CSS가 참조하는 파일입니다
      (템플릿 CSS는 <code>images/base/</code>의 SVG 132개를 참조합니다). 자체 아이콘으로 교체하려면
      <b>CSS가 참조하는 파일을 교체</b>합니다. 클래스 이름은 유지하고 값만 변경하는 3.4와 동일한 방식입니다.</li>
      <li>배포 묶음의 <code>css/images/</code>에 템플릿 아이콘 156개가 포함되어 있습니다. 자체 아이콘 제작 시
      크기·여백 기준으로 참조할 수 있습니다.</li>
    </ul>

    <h2>21.6 변환 실행 및 확인</h2>
    <p>디자인 전달 후 변환은 AICraft 화면에서 실행합니다. 디자이너가 확인해야 하는 사항은 다음과 같습니다 —
    <b>자동매핑은 판정 결과를 즉시 적용하지 않고, 각 레이어의 컴포넌트 판정 결과를 모달로 표시합니다</b>(2.6).</p>
    <ul class="pts">
      <li>해당 목록에서 <b>의도와 다르게 판정된 항목</b>을 먼저 확인합니다. 2.3의 오매핑을 이 단계에서 식별할 수 있습니다.</li>
      <li>적용 후에는 <b>19.5의 항목</b>을 변환 결과에서 확인합니다. Figma 캔버스에서 확인되지 않는 항목입니다.</li>
    </ul>
    <div class="note">실행 화면의 버튼 위치와 단계는 도구 버전에 따라 달라질 수 있습니다.
    본 가이드는 <b>디자인 작성 규칙</b>을 다루며, 실행 절차는 도구 안내를 따릅니다.</div>

    <h2>21.7 용어집</h2>
    <div class="tblwrap">
      <table class="clstbl">
        <tr><th>용어</th><th>정의</th></tr>
        <tr><th scope="row">컴포넌트</th><td>변환기가 인식하는 요소의 종류. 레이어 이름의 <b>두 번째 조각</b>으로 지정합니다(21.1)</td></tr>
        <tr><th scope="row">클래스</th><td>형태를 결정하는 식별자. 레이어 이름의 <b>세 번째 조각 이후</b>이며, CSS가 해당 이름으로 값을 적용합니다(21.2)</td></tr>
        <tr><th scope="row">조각</th><td>레이어 이름을 <code>_</code>로 분리한 단위. <code>search_group_schbox</code>는 세 개의 조각으로 구성됩니다. 상태 단어(<code>disabled</code> 등) 탐색 시에만 공백 등 다른 구분자도 적용됩니다(15.1)</td></tr>
        <tr><th scope="row">폴백</th><td>컴포넌트 조각이 21.1과 불일치할 때 이름 전체에서 인식 가능한 단어를 탐색해 추정하는 동작(2.3)</td></tr>
        <tr><th scope="row">인라인 스타일</th><td>XML에 직접 출력되는 <code>style</code> 값. <b>클래스보다 우선 적용</b>되므로 CSS로 재정의할 수 없습니다(5.1)</td></tr>
        <tr><th scope="row">어댑티브</th><td>표·탭을 좁은 폭에 맞춰 다르게 배치하는 방식. 표는 <b>폭과 무관하게 항상</b> 적용됩니다(10.2)</td></tr>
        <tr><th scope="row">채움(Fill) / 고정(Fixed)</th><td>Figma의 가로·세로 사이징 설정. 결과에 반영되는 대상은 <code>col_fix</code> 컨테이너뿐이며 — <b>폼 필드에서는 영향이 없습니다</b>(5.2 · 6.2)</td></tr>
        <tr><th scope="row">자동매핑</th><td>레이어 이름을 기준으로 컴포넌트를 판정하는 기능. 모달에서 확인 후 적용합니다(2.6)</td></tr>
        <tr><th scope="row">프레임워크 생성</th><td>화면 실행 시 컴포넌트가 자체적으로 형태를 생성하는 방식. 내부를 상세히 작성할 필요가 없습니다(4.2)</td></tr>
      </table>
    </div>
  `,
}
