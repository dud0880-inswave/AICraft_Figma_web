// 모바일판 서술형 챕터 본문. 키는 data-m/nav.js 의 챕터 id.
// h2/h3가 우측 목차로 자동 수집되므로 섹션은 반드시 h2로 연다.
window.PAGES = {
  intro: `
    <h2>1.1 Figma 기반 화면 생성 원리</h2>
    <p class="lead">본 가이드는 Figma 디자인이 변환기를 거쳐 WebSquare 화면으로 전환되는 프로세스를
    다룹니다. <b>모바일 화면은 폭 375px을 기준</b>으로 작성합니다.</p>
    <p>변환기는 사전 정의된 규칙에 따라 작동하는 프로그램으로, 레이어 <b>이름</b>·<b>클래스</b>·<b>중첩 구조</b>의
    세 가지 데이터만을 기반으로 화면을 구성합니다. 세 데이터에 명시되지 않은 정보는 추론하지 않습니다.
    <b>이 원리 자체는 PC와 동일합니다.</b> 이름 규칙(2장)과 구조 규칙(4장)의 문법도 PC와 같습니다.</p>
    <p>모바일에서 달라지는 것은 <b>치수·색·레이아웃</b>입니다. 화면 폭이 375로 고정되어 있으므로 가로로
    늘어놓던 배치가 세로로 쌓이고, 블록의 폭·여백·간격이 375를 기준으로 다시 정해집니다. 이 값들은
    5장에서 다룹니다.</p>
    <p>특히 레이어 이름은 변환기가 각 컴포넌트의 유형을 식별하는 유일한 기준입니다.
    명명 규칙을 준수해야 정확한 컴포넌트 변환이 가능하며, <b>규격에 맞지 않는 이름은 의도와 다른
    컴포넌트로 변환되거나 변환 대상에서 제외됩니다.</b></p>

    <h2>1.2 역할 분담</h2>
    <p>화면 하나가 생성되기까지의 책임은 세 주체로 구분됩니다. <b>구분 방식은 PC와 동일합니다.</b></p>
    <div class="tblwrap">
      <table>
        <thead>
          <tr><th>담당</th><th>책임지는 것</th><th>참조</th></tr>
        </thead>
        <tbody>
          <tr><td><b>디자이너</b></td><td>레이어 이름, 클래스, 구조</td><td>2~4장</td></tr>
          <tr><td><b>변환기</b></td><td>셀 속성·표 뼈대 등 구조 보강</td><td>4장</td></tr>
          <tr><td><b>CSS</b></td><td>색과 치수</td><td>3장(클래스)이 연결 지점</td></tr>
        </tbody>
      </table>
    </div>
    <p>디자이너가 정의하는 대상은 <b>이름·클래스·구조</b>로 한정됩니다. 색상 일치나 픽셀 단위
    치수 조정은 CSS가 담당하므로 디자이너의 작업 범위에 포함되지 않습니다. 다만 어떤 레이어에
    어떤 클래스를 지정했는지는 CSS가 값을 적용할 대상을 식별하는 유일한 기준이므로 정확하게
    작성해야 합니다.</p>
    <p>모바일에서는 <b>CSS가 담당하는 몫이 PC보다 큽니다.</b> 카드 배경과 여백, 검색영역의 세로 배치,
    제목줄 우측 버튼의 아이콘 전용 형태는 모두 클래스와 구조를 읽어 CSS가 그립니다. 디자이너는
    그 결과와 같은 모습을 Figma에 그리고, 이름·클래스·구조를 규격대로 맞춰 두면 됩니다.</p>
    <div class="note">세 요소 중 하나가 누락되어도 화면 생성 자체는 실패하지 않으며,
    <b>의도와 다른 결과</b>가 생성됩니다. 본 가이드의 상당 부분은 이러한 결과 불일치가
    발생하는 지점을 사전에 안내하는 데 할애되어 있습니다.</div>

    <h2>1.3 모바일만의 전제 3가지</h2>
    <p class="lead">2장으로 넘어가기 전에 <b>모바일 화면에만 적용되는 전제 세 가지</b>를 확인합니다.
    이 세 가지는 이름·클래스·구조보다 앞서는 조건이며, 어긋나면 이후 규칙을 모두 지켜도 결과가
    통째로 어긋납니다.</p>

    <h3>① Mobile 변수모드가 걸린 컨테이너 안에서 그린다</h3>
    <p>컴포넌트의 치수와 색은 <code>ws</code> 변수 컬렉션에 바인딩되어 있고, 이 컬렉션은
    <b>PC · Mobile 두 축</b>을 갖습니다. <b>모드가 지정되지 않은 프레임에서 컴포넌트를 꺼내 쓰면
    전부 PC 값으로 풀립니다.</b></p>
    <div class="warn"><b>모드를 지정하는 별도의 조작은 없습니다 — 상속받아야 합니다.</b>
    새 모바일 화면은 <b>디자인 템플릿의 모바일 섹션(<code>template_Mobile</code>) 안에서</b> 만들고,
    작업이 끝난 뒤에도 그 안에 둡니다.<br>
    실제 사고 사례입니다. 빈 프레임에서 작업했더니 화면 타이틀바 높이가 24(정답 60), 셀렉트가
    높이 24·모서리 2·글자 12(정답 48·8·16)로 <b>전부 PC 값</b>이 나왔습니다. 눈으로는 "조금 작게
    나왔네" 정도로만 보여 원인을 찾는 데 오래 걸립니다.</div>
    <div class="note"><b>가장 빠른 자가진단은 폼 필드입니다.</b> 입력칸·셀렉트의 높이가
    <b>48</b>이고 모서리가 <b>8</b>이면 모바일 모드입니다. 높이 24·모서리 2면 PC로 풀린 것입니다.</div>

    <h3>② 값은 컴포넌트 → 디자인 템플릿 → CSS 순서로 찾는다</h3>
    <p>어떤 값을 어디서 가져올지는 순서가 정해져 있습니다.</p>
    <div class="tblwrap">
      <table>
        <thead>
          <tr><th>무엇</th><th>어디서 가져오나</th></tr>
        </thead>
        <tbody>
          <tr><td>컴포넌트 종류 · 트리 구조 · <b>레이어 이름</b> · <b>클래스</b></td><td>정본 XML</td></tr>
          <tr><td>크기 · 색 · 모서리 · 여백 · 폰트 등 <b>스타일</b></td><td><b>Figma</b> (컴포넌트 → 디자인 템플릿)</td></tr>
        </tbody>
      </table>
    </div>
    <div class="warn"><b>CSS 파일에서 스타일 값을 읽으면 틀립니다.</b> 실제 불일치가 남아 있습니다 —
    달력의 모서리는 CSS에 <b>4</b>로 적혀 있으나 Figma 마스터의 정답은 <b>16</b>입니다.
    버튼의 높이·좌우여백도 CSS 쪽 값이 Figma보다 낡아 있습니다.<br>
    CSS는 <b>"이 요소에 어떤 클래스가 붙는가"를 확인하는 용도</b>로만 사용합니다.</div>
    <div class="note"><b>값을 추정하지 않습니다.</b> 색·높이·너비·여백·모서리·아이콘·폰트는 전부
    출처에서 직접 읽습니다. "그럴듯한 기본값"은 사용하지 않습니다. 이 산출물은 다른 사람에게
    배포되므로, 한 사람의 추정이 배포받은 전원의 오류가 됩니다.</div>

    <h3>③ 375를 넘는 값이 나오면 버그다</h3>
    <p>모바일 화면의 폭은 375입니다. 어떤 레이어든 <b>폭이 375를 넘으면 그것은 버그</b>입니다.
    <b>유일한 예외는 그리드</b>로, 그리드는 컬럼 수만큼 넓어지고 넘치는 만큼이 실행 시
    <b>가로 스크롤</b>이 됩니다.</p>
    <div class="warn"><b>눈으로는 잡히지 않습니다.</b> 최상위 프레임이 내용을 잘라내도록 설정되어
    있으면 375를 넘어간 부분이 <b>화면 밖으로 잘려서 보이지 않습니다.</b> 화면을 보고 판단하지 말고
    레이어의 실제 폭 값으로 확인합니다.</div>
  `,
  naming: `
    <h2>2.1 이름의 세 조각</h2>
    <p class="lead">레이어 이름은 <b>밑줄(<code>_</code>)로 구분한 3단어</b>로 짓습니다.
    <code>접두어_컴포넌트_클래스</code> 순서입니다. <b>이 규칙은 PC와 완전히 동일합니다.</b></p>
    <p>예를 들어 조회 조건을 담는 영역이라면 <code>search_group_schbox</code>처럼 짓습니다.
    앞의 <code>search</code>는 "이게 뭐 하는 영역인지" 사람이 알아보라고 붙이는 접두어이고,
    가운데 <code>group</code>이 변환기에게 "이건 그룹 컴포넌트다"라고 알려주는 이름이고,
    뒤의 <code>schbox</code>는 그 컴포넌트에 입힐 클래스(모양)입니다.</p>
    <pre>search_group_schbox
  │      │      └ 클래스 — 두 번째 밑줄 <b>뒤 전체</b>
  │      └ 컴포넌트 — 변환기의 매핑 키
  └ 접두어 — 사람이 알아보라고 붙이는 이름(자유)</pre>
    <ul class="pts">
      <li><b>접두어</b> — 의미 식별용이며 변환기는 참조하지 않습니다. 자유롭게 지정할 수 있습니다.</li>
      <li><b>컴포넌트</b> — <code>_</code>로 분리했을 때 <b>두 번째 조각</b>이며, 변환기가 유형 판정에
      사용하는 값입니다.</li>
      <li><b>클래스</b> — <b>두 번째 <code>_</code> 뒤의 전체</b>가 클래스로 처리됩니다.</li>
    </ul>
    <div class="note">장식용 말단 레이어(아이콘·점·라벨 텍스트)는 매핑 대상이 아니므로 이름을
    자유롭게 지어도 됩니다. 규칙을 지켜야 하는 것은 <b>컴포넌트로 변환될 레이어</b>입니다.</div>

    <h2>2.2 클래스는 공백으로 나열</h2>
    <p>클래스가 여러 개인 경우 <b>공백으로 구분해 나열</b>합니다. 밑줄로 잇거나 쉼표로 구분하지
    않습니다.</p>
    <pre>search_button_btn_cm fill pt search
              └───────────────────┘
              클래스 4개 = btn_cm · fill · pt · search</pre>
    <div class="warn"><b>XML의 클래스를 하나도 빠뜨리지 않고 전부 옮깁니다.</b>
    하나라도 빠지면 CSS가 그 요소를 찾지 못해 렌더 결과가 깨집니다. 모바일은 카드 배경·여백·
    아이콘 전용 버튼 같은 형태를 전부 CSS가 그리므로, 클래스 누락의 영향이 PC보다 크게 나타납니다.</div>

    <h2>2.3 단일 토큰 금지</h2>
    <p class="lead"><b>밑줄이 없는 한 단어짜리 이름은 사용하지 않습니다.</b></p>
    <p>이름을 <code>_</code>로 분리해 두 번째 조각을 읽는 방식이므로, <code>th</code>·<code>td</code>처럼
    한 단어로만 지으면 <b>두 번째 조각이 존재하지 않아 매핑에 실패합니다.</b> 접두어를 붙여
    최소 두 조각으로 만듭니다.</p>
    <div class="tblwrap">
      <table>
        <thead>
          <tr><th>쓰면 안 되는 이름</th><th>이렇게 씁니다</th><th>무엇</th></tr>
        </thead>
        <tbody>
          <tr><td><code>th</code></td><td><code>cond_th</code></td><td>조회 조건의 라벨 셀</td></tr>
          <tr><td><code>td</code></td><td><code>field_td</code></td><td>필드가 들어가는 값 셀</td></tr>
        </tbody>
      </table>
    </div>

    <h2>2.4 상태는 노드명에만 적는다</h2>
    <p class="lead">비활성·읽기전용·필수·에러·켜짐/꺼짐 같은 <b>상태는 레이어 이름의 클래스 자리에
    적습니다.</b></p>
    <div class="warn"><b>Figma에서 컴포넌트의 변형(variant)을 골라 둔 것은 변환에 실리지 않습니다.</b>
    변형 선택은 Figma 안에서만 유효한 정보입니다. 변환기까지 전달되는 유일한 경로는
    <b>레이어 이름</b>입니다. 스위치를 켠 모양으로 그려 두어도, 이름에 <code>on</code>이 없으면
    결과는 꺼진 상태입니다.</div>
    <p>상태 값은 클래스로 새어 나가지 않고 <b>속성으로 변환</b>됩니다. 다음은 실제 변환 결과입니다.</p>
    <div class="tblwrap">
      <table>
        <thead>
          <tr><th>레이어 이름</th><th>변환 결과</th></tr>
        </thead>
        <tbody>
          <tr><td><code>sw_checkbox_input_switch off</code></td>
          <td><code>class="input_switch"</code> — <code>off</code>는 상태 토큰이라 클래스로 새지 않습니다</td></tr>
          <tr><td><code>swon_checkbox_input_switch on</code></td>
          <td><code>class="input_switch"</code> + 선택 상태(켜짐)</td></tr>
          <tr><td><code>f_checkbox_req</code></td><td><code>class="req"</code></td></tr>
          <tr><td><code>swdis_checkbox_input_switch off disabled</code></td>
          <td><code>class="input_switch"</code> + <code>disabled="true"</code></td></tr>
        </tbody>
      </table>
    </div>
    <div class="note"><b>상태 토큰 목록</b> — <code>base</code> · <code>disabled</code> · <code>dis</code> ·
    <code>readonly</code> · <code>ro</code> · <code>req</code> · <code>required</code> · <code>필수</code> ·
    <code>error</code> · <code>err</code> · <code>에러</code> · <code>on</code> · <code>off</code><br>
    이 값들은 클래스가 아니라 <b>속성</b>으로 변환됩니다.</div>

    <h2>2.5 calendar·slider 는 클래스 자리가 다르다</h2>
    <div class="warn"><b>달력·슬라이더·날짜선택기 계열은 세 번째 조각에 CSS 클래스를 쓰지 않습니다.</b>
    이 컴포넌트들의 세 번째 조각은 클래스가 아니라 <b>변형 지정자</b>입니다 —
    <code>yearMonthDate</code>(연·월·일 단위) · <code>native</code>(단말 기본 형태) ·
    <code>bg</code>(배경 형태)와 같은 값이 들어갑니다.</div>
    <p>이 자리에 CSS 클래스를 적으면 클래스로 새어 나가 정본과 어긋납니다. 어떤 변형 지정자를
    쓰는지는 컴포넌트별 챕터에서 다룹니다 — 달력은 <b>16장</b>, 슬라이더를 포함한 그 밖의
    컴포넌트는 <b>18장</b>입니다.</p>
    <div class="note">2.1에서 본 <b>일반 규칙(세 번째 조각 = 클래스)의 예외</b>는 이 계열뿐입니다.
    그 밖의 컴포넌트는 전부 세 번째 조각이 클래스입니다.</div>
  `,
  class: `
    <h2>3.1 클래스는 CSS가 값을 찾는 유일한 이름표</h2>
    <p class="lead">클래스는 <b>레이어 이름의 두 번째 <code>_</code> 뒤 전부</b>입니다(2.1).
    이 이름표가 있어야 CSS가 그 요소에 색과 치수를 입힙니다.</p>
    <p>모바일에서 이 연결이 특히 중요한 이유는, <b>화면의 형태 상당 부분을 CSS가 직접 그리기</b>
    때문입니다. 카드의 흰 배경과 여백, 검색영역의 세로 배치, 제목줄 우측 버튼의 아이콘 전용
    형태는 Figma 레이어를 하나하나 옮겨 만든 것이 아니라 <b>클래스와 구조를 읽어 CSS가 만든 것</b>입니다.</p>
    <div class="warn"><b>클래스를 비워 두지 않습니다.</b> 클래스가 없으면 기준이 되는 정의가 없어,
    해당 레이어의 폭·높이·색이 <b>Figma 측정값 그대로 결과에 박힙니다.</b> 화면상으로는 같아 보여도
    그 값은 <b>CSS로 수정할 수 없는 상태로 고정</b>되며, 이후 수정하려면 레이어를 개별적으로 찾아
    고쳐야 합니다.</div>
    <div class="note"><b>예외가 하나 있습니다 — 영역을 묶는 그룹에는 클래스를 주지 않습니다.</b>
    모바일의 카드는 클래스가 아니라 <b>구조</b>로 인식되기 때문입니다. 4장에서 다룹니다.</div>

    <h2>3.2 표 클래스는 빠뜨리면 안 된다</h2>
    <p class="lead">표에는 <b>반드시 붙여야 하는 클래스가 두 개</b> 있습니다. 없으면 경고 없이
    형태만 무너집니다.</p>
    <div class="tblwrap">
      <table>
        <thead>
          <tr><th>클래스</th><th>붙일 레이어</th><th>없으면</th></tr>
        </thead>
        <tbody>
          <tr><td><code>w2tb_tr</code></td><td><b>모든 행(tr)</b></td>
          <td>어댑티브 표에서 라벨 셀과 값 셀이 각각 줄바꿈되어 <b>[라벨 / 입력] 짝이 깨집니다</b></td></tr>
          <tr><td><code>w2tb_adaptive_layout</code></td><td><b>어댑티브 표에만</b></td>
          <td>어댑티브 동작이 아예 걸리지 않습니다</td></tr>
        </tbody>
      </table>
    </div>
    <div class="warn"><code>w2tb_adaptive_layout</code>은 <b>자동으로 붙지 않습니다.</b>
    라벨과 값을 한 쌍씩 세로로 쌓는 어댑티브 표를 쓰려면 표 레이어 이름에 <b>직접 지정</b>해야 합니다.
    누락 시 경고 없이 일반 표로 변환됩니다. 배치 변경 방식은 <b>10장</b>에서 다룹니다.</div>
    <div class="note">모바일은 화면 폭이 375로 좁아 <b>라벨을 왼쪽에 두고 값을 오른쪽에 두는 배치가
    성립하지 않는 경우가 많습니다.</b> 표를 세로로 쌓는 어댑티브 배치를 쓰는 빈도가 PC보다 높고,
    그만큼 위 두 클래스의 누락이 곧바로 화면 붕괴로 이어집니다.</div>

    <h2>3.3 클래스 목록</h2>
    <p>클래스 이름은 <b>신규로 정의하지 않고 지정된 목록의 값을 그대로 사용합니다.</b>
    <b>클래스 어휘는 PC와 모바일이 동일합니다</b> — 같은 이름에 모바일용 값이 연결되어 있을 뿐입니다.</p>
    <div class="note"><b>전체 클래스 목록은 <b>21장 부록</b>에 있습니다.</b> 디자인 템플릿에서 실제
    사용 중인 클래스를 층별로 정리해 두었습니다. 목록에 없더라도 <b>컴포넌트별 챕터(6~18장)에서
    안내하는 클래스는 사용할 수 있습니다.</b> 반면 <b>부록과 챕터 어디에도 없는 이름은 변환기가
    인식하지 않습니다.</b></div>
    <p>자체 디자인 시스템의 CSS를 쓰는 경우에도 <b>클래스 이름은 표준 값을 그대로 사용합니다.</b>
    변경 대상은 그 클래스에 연결된 CSS의 <b>값</b>에 한정됩니다. <b>이름은 규약이고 값은 자체 정의
    대상입니다.</b></p>
  `,
  structure: `
    <h2>4.1 영역은 클래스 없는 group 으로 묶는다</h2>
    <p class="lead">모바일 화면은 제목과 내용이 한 덩어리씩 카드로 그려집니다.
    그 덩어리를 만드는 것은 <b>클래스 없는 그룹</b>입니다. <b>모바일 고유 규칙입니다.</b></p>
    <p>영역을 감싸는 그룹에는 <b>클래스를 주지 않습니다.</b> 레이어 이름의 두 번째 조각만
    <code>group</code>으로 두고, 세 번째 조각(클래스 자리)은 비웁니다.</p>
    <pre>grp_group                 ← 클래스 없는 그룹
├ grp_group_titbox        ← 영역 제목
└ grp_group_gvwbox        ← 영역 내용</pre>
    <div class="note"><b>왜 클래스를 안 주나요?</b><br>
    CSS가 <b>구조를 읽어서</b> 카드를 그리기 때문입니다. 제목(<code>titbox</code>)을 직속 자식으로
    품은 그룹을 찾아 흰 배경과 여백을 입힙니다. 그래서 이름표가 필요 없습니다.<br>
    또한 이 자리에 쓸 만한 컨테이너 클래스가 <b>정본 XML에도 CSS에도 없습니다.</b>
    변환기가 없는 클래스를 만들어 내면 <b>「XML과 동일한 구조」라는 기준</b>이 깨지므로,
    클래스를 창작하지 않고 <b>구조만으로</b> 성립시키는 방식을 택했습니다.</div>
    <p>클래스 없는 그룹은 <b>정본 XML에 실제로 존재하는 형태</b>입니다. 새로 만들어 낸 규칙이
    아니라, 이미 쓰이고 있는 구조에 CSS를 맞춘 것입니다.</p>

    <h2>4.2 titbox 가 영역의 시작점</h2>
    <p><code>titbox</code>가 나오면 <b>다음 <code>titbox</code> 직전까지</b>가 한 영역입니다.
    영역이 두 개면 클래스 없는 그룹도 두 개입니다.</p>
    <pre>scr_group_sub_contents        ← 화면 최상위 그룹(화면에 하나만)
├ pgt_group_pgtbox            ← 화면 타이틀바 — 항상 맨 위
├ search_group_schbox         ← 조회영역
├ <b>grp_group</b>                 ← 영역 1 (클래스 없는 그룹)
│  ├ grp_group_titbox
│  └ grp_group_gvwbox
├ <b>grp2_group</b>                ← 영역 2 (클래스 없는 그룹)
│  ├ grp2_group_titbox
│  └ grp2_group_tblbox
└ act_group_btnbox            ← 하단 액션 버튼</pre>
    <div class="note"><b>PC와 다른 점입니다.</b> PC는 제목과 내용을 <b>같은 레벨</b>에 늘어놓고
    제목이 나올 때마다 새 영역이 시작되는 방식이지만, <b>모바일은 제목과 내용을 그룹으로 한 번 더
    묶습니다.</b> 그 묶음이 곧 카드가 되기 때문입니다.</div>
    <div class="warn"><b>제목을 그룹 밖에 두면 카드가 생기지 않습니다.</b>
    <code>titbox</code>는 반드시 클래스 없는 그룹의 <b>직속 자식</b>이어야 합니다.
    한 겹 더 감싸면 CSS가 찾지 못합니다.</div>

    <h2>4.3 카드는 CSS가 그린다</h2>
    <p class="lead">구조만 맞으면 <b>흰 배경 · 안쪽 여백 · 화면 끝까지 늘어나는 폭</b>이 자동으로
    붙습니다. 디자이너가 배경 사각형을 따로 깔 필요가 없습니다.</p>
    <p>4.1의 구조가 성립하면 CSS가 그 그룹에 다음을 입힙니다.</p>
    <ul class="pts">
      <li><b>흰 배경</b> — 카드 사이는 회색 바탕이 드러나 영역이 구분됩니다.</li>
      <li><b>안쪽 여백 상하 24 · 좌우 20</b> — 카드 안 내용이 좌우 20씩 들어와 폭 335가 됩니다.</li>
      <li><b>화면 끝까지 늘어나는 폭 375</b> — 카드 자체는 좌우 여백을 뚫고 화면 끝까지 갑니다.</li>
      <li><b>내용 사이 간격 16</b> — 카드 안 기본 간격입니다(5.3).</li>
    </ul>
    <div class="warn"><b>Figma에는 결과와 같은 모습으로 그립니다.</b> CSS가 그려 준다고 해서 Figma를
    비워 두지 않습니다. 인수자가 확인하는 것은 결과 XML이 아니라 <b>Figma 화면</b>이므로,
    카드는 <b>폭 375 · 흰 배경 · 안쪽 여백 24/20</b>으로 실제 결과와 같게 그립니다. 값은 5장에 있습니다.</div>

    <h2>4.4 변환기가 보강하는 것</h2>
    <div class="note">
    <p>표·그리드를 작성할 때 다음 항목은 <b>직접 작성하지 않아도</b> 변환기가 자동으로 생성합니다.
    <b>이 목록은 PC와 동일합니다.</b></p>
    <ul class="pts">
      <li><b>셀이 아닌 하위 항목의 셀 래핑</b> — 행 레이어 직속에 셀(th/td)이 아닌 레이어가
      포함되어도 변환기가 셀로 감싸 표 구조를 유지합니다.</li>
      <li><b>셀 속성 블록</b> — 셀의 헤더 여부, 병합 셀 수(가로·세로) 등의 속성을 변환기가
      자동으로 생성합니다.</li>
      <li><b>표의 열 너비 정의</b> — 열 너비를 정의하는 레이어를 별도로 작성하지 않아도 변환기가
      각 셀의 측정 너비를 기준으로 자동 생성합니다.</li>
      <li><b>그리드 영역 보정</b> — 그리드 영역에 그리드 레이어 없이 데이터 행만 배치된 경우,
      변환기가 그리드를 삽입해 구조를 보정합니다.</li>
    </ul>
    <p>표·그리드에서는 <b>셀 내용과 이름·클래스</b>만 작성하면 되며, 연결 구조는 변환기가
    담당합니다.</p>
    </div>
    <div class="warn"><b>행 클래스는 예외로 취급합니다.</b> 변환기가 행 클래스를 보강하기는 하지만,
    모바일 정본은 <code>w2tb_tr</code>을 <b>모든 행에 직접 적도록</b> 정하고 있습니다(3.2).
    자동 보강에 의존하지 말고 이름에 적어 둡니다.</div>
    <div class="note">Figma에서 <b>표시 아이콘을 해제한 숨김 레이어는 결과에 나오지 않습니다.</b>
    화면에서만 감춰지는 것이 아니라 변환기가 완전히 건너뜁니다. 상위를 숨기면 하위도 전부
    함께 빠집니다. 전달 전 레이어 패널에서 확인해야 하는 항목이며, 점검 절차는 <b>19장</b>에서 다룹니다.</div>
  `,
  autolayout: `
    <h2>5.1 375 · 20 · 335</h2>
    <p class="lead">모바일의 모든 계산은 이 세 숫자에서 출발합니다.
    <b>화면 375, 좌우 여백 20, 그래서 콘텐츠 335</b>입니다.</p>
    <div class="tblwrap">
      <table>
        <thead>
          <tr><th>항목</th><th>값</th><th>설명</th></tr>
        </thead>
        <tbody>
          <tr><td>화면 폭</td><td><b>375</b></td><td>모바일 단말 기준폭</td></tr>
          <tr><td>좌우 페이지 여백</td><td><b>20</b></td><td>화면 양쪽 여백</td></tr>
          <tr><td><b>콘텐츠 폭</b></td><td><b>335</b></td><td>375 − 20 × 2</td></tr>
          <tr><td>기본 간격</td><td><b>8</b></td><td>요소 사이 기본값</td></tr>
          <tr><td>촘촘한 간격</td><td><b>4</b></td><td><b>좁은 배치</b> 버튼 안 아이콘과 라벨 사이처럼 바짝 붙는 자리
          — 자기 행을 차지하는 넓은 배치 버튼은 <b>8</b>입니다(13.1)</td></tr>
          <tr><td>폰트</td><td><b>Pretendard</b></td><td>—</td></tr>
          <tr><td>브랜드 색(프라이머리)</td><td><b>#237AF3</b></td><td>PC는 <code>#256EF4</code> — <b>값이 다릅니다</b></td></tr>
        </tbody>
      </table>
    </div>
    <div class="warn"><b>375를 넘는 값이 나오면 버그입니다.</b> 유일한 예외는 그리드로,
    컬럼 수만큼 넓어지고 넘치는 만큼이 실행 시 가로 스크롤이 됩니다(<b>11장</b>).
    최상위 프레임이 내용을 잘라내면 넘친 부분이 <b>보이지 않으므로</b>, 눈이 아니라 폭 값으로 확인합니다.</div>

    <h2>5.2 화면 끝까지 vs 안쪽 여백</h2>
    <p class="lead">모바일에서는 일부 블록이 <b>좌우 여백을 뚫고 화면 끝까지</b> 늘어납니다.
    Figma는 보이는 대로 그리는 도구이므로, <b>실제 렌더 결과의 폭 그대로</b> 그립니다.</p>
    <div class="tblwrap">
      <table>
        <thead>
          <tr><th>블록</th><th>Figma 폭</th><th>안쪽 여백 → 콘텐츠</th></tr>
        </thead>
        <tbody>
          <tr><td><code>pgtbox</code> 화면 타이틀바</td><td><b>375</b> 화면 끝까지</td><td>0 20 → <b>335</b></td></tr>
          <tr><td><code>schbox</code> 조회영역</td><td><b>375</b> 화면 끝까지</td><td>24 20 → <b>335</b></td></tr>
          <tr><td>카드(클래스 없는 그룹, 4장)</td><td><b>375</b> 화면 끝까지</td><td>24 20 → <b>335</b></td></tr>
          <tr><td><code>gvwbox</code> 그리드 영역</td><td><b>375</b> 화면 끝까지</td><td>0 20 → 그리드가 좌 20에서 시작, 넘치면 가로 스크롤</td></tr>
          <tr><td>그 밖 — <code>titbox</code> · <code>btnbox</code> · <code>tblbox</code> · <code>tbcbox</code> …</td>
          <td><b>335</b> 카드 안 인셋</td><td>—</td></tr>
        </tbody>
      </table>
    </div>
    <div class="warn"><b>안쪽 여백을 이중으로 주지 않습니다.</b> 좌우 20 인셋은 <b>카드가 담당</b>합니다.
    카드 안의 <code>titbox</code>·<code>tblbox</code>에 20을 또 주면 콘텐츠가 295로 줄어
    다른 블록과 줄이 맞지 않습니다. 카드 안 블록의 좌우 여백은 <b>0</b>입니다.</div>

    <h2>5.3 블록 사이 간격</h2>
    <p class="lead">간격은 <b>어느 층에 있는 무엇끼리인가</b>로 결정됩니다. 네 가지뿐입니다.</p>
    <div class="tblwrap">
      <table>
        <thead>
          <tr><th>관계</th><th>간격</th></tr>
        </thead>
        <tbody>
          <tr><td><b>최상위 자식끼리</b> — 타이틀바 · 조회영역 · 카드 · 하단 버튼</td><td><b>10</b></td></tr>
          <tr><td><b>카드 안</b> 기본</td><td><b>16</b></td></tr>
          <tr><td>카드 안 <b>블록 ↔ 블록</b> — 그리드·표·탭·트리·아코디언·달력끼리</td><td><b>32</b></td></tr>
          <tr><td>카드 안 <b>블록 ↔ 뒤따르는 부속</b> — 페이징 · 하단 버튼</td><td><b>8</b></td></tr>
        </tbody>
      </table>
    </div>
    <div class="warn"><b>최상위 10과 카드 안 16을 혼동하지 않습니다.</b>
    최상위 자식 사이는 <b>10</b>이 맞습니다. 그런데 오토레이아웃을 새로 만들면 간격 기본값도 10이라서,
    <b>카드 안에 10이 남아 있으면 "값을 안 넣은 것"</b>입니다. 카드 안은 <b>16</b>이어야 합니다.
    카드·내부 컨테이너의 간격은 반드시 값을 명시합니다.</div>
    <div class="note"><b>32와 8은 어떻게 나오나요?</b><br>
    카드 안 기본 간격 16에 위쪽 여백이 더해지거나 빠져 만들어집니다 — 블록끼리는 16 + 16 = <b>32</b>로
    벌어져 영역 경계를 만들고, 페이징·하단 버튼 같은 부속은 16 − 8 = <b>8</b>로 앞 블록에 붙습니다.
    구분선이나 배경 없이 <b>여백만으로</b> 영역을 나누는 것이 모바일의 문법입니다.</div>

    <h2>5.4 폭은 채우고 높이는 내용에 맡긴다</h2>
    <p class="lead">모바일 오토레이아웃의 기본형은 <b>폭 = 채우기(Fill), 높이 = 내용에 맞춤(Hug)</b>입니다.</p>
    <p>화면 폭이 375로 고정되어 있으므로, 컨테이너는 부모 폭을 채우고 안의 내용이 세로로 쌓이는
    형태가 기본입니다. <b>폼 필드는 한 줄을 다 씁니다</b> — 입력칸·셀렉트는 폭 <b>335</b>로 늘어나며,
    PC처럼 필드마다 다른 폭을 주지 않습니다.</p>
    <ul class="pts">
      <li><b>폭을 채우는 것</b> — 폼 필드 · 조회 버튼 · 하단 액션 버튼 · 그리드 셀</li>
      <li><b>폭을 고정하는 것</b> — 아이콘 · 체크 상자 · 라디오 점처럼 크기가 정해진 요소.
      이때 안쪽 여백은 <b>0</b>으로 둡니다.</li>
      <li><b>높이</b> — 컨테이너는 <b>내용에 맞춤</b>으로 둡니다. 고정 높이를 남기면 내용이
      늘었을 때 잘립니다.</li>
    </ul>
    <div class="warn"><b>가로 배치를 세로로 바꿀 때 높이 고정이 남아 사고가 납니다.</b>
    실제 사례입니다. 좌우 2단이던 영역을 세로로 바꾸자 각 단이 <b>30px로 눌렸습니다.</b>
    가로 배치 시절의 고정 높이가 그대로 남아 있었기 때문입니다.
    <b>배치 방향을 바꿨다면 안쪽 컨테이너까지 전부 높이를 「내용에 맞춤」으로 풀어야 합니다.</b></div>
    <div class="note"><b>어떤 값이 XML로 직접 나가는지에 대한 규칙 자체는 PC와 동일합니다.</b>
    다만 <b>모바일의 폼 필드는 한 줄을 다 쓰는 것이 기본</b>이라 폭이 335가 됩니다 — PC에서 필드마다
    폭을 지정하던 감각을 그대로 가져오면 어긋납니다.<br>
    <b>셀 안에 필드가 하나뿐이면</b> 그 필드의 폭은 셀을 가득 채우는 값으로 덮입니다.
    <b>한 셀에 필드를 둘 이상 배치하면</b> 덮어쓰지 않고 <b>Figma에서 그린 폭이 그대로 나가므로</b>,
    그 경우에는 폭을 결과와 같게 그립니다. 필드 종류별 세부 규칙은 <b>15장</b>에서 다룹니다.</div>
  `,
  checklist: `
    <p class="lead">전달 전 확인 목록입니다. 정본의 최종 체크 항목을 모바일 작성 순서에 맞춰 다시
    묶었습니다. <b>이 장의 원칙은 「눈으로 보고 판단하지 않는다」</b>입니다 — 375를 넘어 잘린 부분도,
    PC 값으로 풀린 치수도 화면만 봐서는 잡히지 않습니다.</p>

    <h2>19.1 넘기기 전에 확인할 것</h2>

    <h3>화면 전체</h3>
    <ul class="pts">
      <li><b>375를 넘는 레이어가 없습니다.</b> 유일한 예외는 그리드이며, 넘치는 만큼은 실행 시
      가로 스크롤이 됩니다(5.1 · 11.1). 최상위 프레임이 내용을 잘라내면 넘친 부분이 보이지 않으므로
      <b>화면이 아니라 폭 값으로</b> 확인합니다.</li>
      <li><b>폼 필드의 높이가 48 · 모서리가 8입니다.</b> 24 · 2로 나왔다면 Mobile 변수모드가 걸리지 않아
      PC 값으로 풀린 것입니다. 가장 빠른 자가진단 지점입니다(1.3 · 15.1).</li>
      <li><b>화면 끝까지 늘어나는 블록과 카드 안 블록이 구분되어 있습니다.</b>
      <code>pgtbox</code> · <code>schbox</code> · 카드 · <code>gvwbox</code>는 <b>375</b>,
      <code>titbox</code> · <code>btnbox</code> · <code>tblbox</code>는 <b>335</b>입니다(5.2).</li>
      <li><b>글꼴이 Pretendard입니다.</b> 한꺼번에 생성한 레이어는 Inter로 남아 있는 경우가 있습니다.</li>
      <li><b>컴포넌트 기본값 텍스트가 남아 있지 않습니다</b> — <code>텍스트</code> · <code>Placeholder</code> ·
      <code>타이틀</code>이 그대로면 인수자가 내용을 확인할 수 없습니다.</li>
      <li><b>표시를 끈 숨김 레이어가 없습니다.</b> 숨긴 레이어는 하위까지 통째로 결과에서 빠집니다(4.4).</li>
    </ul>

    <h3>이름</h3>
    <ul class="pts">
      <li><b>매핑 대상 레이어가 <code>접두어_컴포넌트_클래스</code> 형식입니다</b>(2.1). 밑줄 없는
      한 단어 이름이 없어야 합니다 — <code>th</code> · <code>td</code>에도 접두어를 붙입니다(2.3).</li>
      <li><b>XML의 클래스를 하나도 빠뜨리지 않고 전부 옮겼습니다.</b> 클래스가 여럿이면 공백으로
      나열합니다(2.2).</li>
      <li><b>상태가 노드명에 적혀 있습니다</b> — <code>req</code> · <code>disabled</code> ·
      <code>readonly</code> · <code>error</code> · <code>on</code> / <code>off</code>.
      Figma에서 변형을 골라 둔 것은 변환에 실리지 않습니다(2.4). 스위치를 켠 모양으로 그려도
      이름에 <code>on</code>이 없으면 결과는 꺼진 상태입니다.</li>
      <li><b><code>calendar</code> · <code>slider</code> 계열은 세 번째 조각이 변형 지정자입니다</b> —
      <code>yearMonthDate</code> · <code>native</code>처럼 씁니다. 이 자리에 CSS 클래스를 적으면
      결과가 정본과 어긋납니다(2.5 · 16.2).</li>
      <li><b>그리드·트리의 구조 이름이 규격에 맞습니다</b> — 헤더 행 이름에 <code>header</code>,
      데이터 행 이름에 <code>row</code>(11.1), 트리 노드는 두 번째 조각이 <code>node</code>와
      완전 일치(12.1)입니다.</li>
    </ul>

    <h3>클래스</h3>
    <ul class="pts">
      <li><b>모든 행에 <code>w2tb_tr</code>이 있습니다.</b> 빠지면 라벨 셀과 값 셀이 각각 줄바꿈되어
      [라벨 / 입력] 짝이 깨집니다(3.2 · 10.1).</li>
      <li><b><code>w2tb_adaptive_layout</code>은 어댑티브 표에만 있습니다.</b> 자동으로 붙지 않으므로
      직접 지정하며, 일반 표에 붙이지 않습니다(3.2 · 10.2).</li>
      <li><b>영역을 감싼 그룹에는 클래스가 없습니다.</b> 모바일의 카드는 클래스가 아니라 구조로
      인식됩니다 — <code>titbox</code>를 직속 자식으로 품은 무클래스 그룹이 카드가 됩니다(4.1 · 4.3).</li>
      <li><b>형태가 클래스로 결정되는 레이어에 클래스가 있습니다</b> — 컨테이너 · 버튼 · 제목 · 필드.
      비워 두면 Figma 측정값이 그대로 박혀 CSS로 수정할 수 없게 됩니다(3.1).</li>
      <li><b>버튼 클래스 조합 순서가 규격에 맞습니다</b> — <code>btn_cm</code> → 변형 →
      <code>icon</code> → <code>disabled</code>(13.1).</li>
    </ul>

    <h3>구조와 컴포넌트</h3>
    <ul class="pts">
      <li><b>아이콘 전용 버튼에 텍스트 자식이 없습니다.</b> 클래스에 <code>icon</code>을 넣고 안을
      비워야 정사각 아이콘 버튼(48 × 48)이 됩니다(13.1).</li>
      <li><b>글자가 필요한 버튼에는 텍스트 자식이 있습니다.</b> 변환기는 버튼 라벨을
      <b>텍스트 자식에서만</b> 읽습니다(13.1).</li>
      <li><b>아이콘이 버튼 용도와 맞습니다.</b> 복제해 만든 저장 버튼에 돋보기가 남아 있는 식의
      잔재가 흔합니다. 아이콘 그림은 클래스가 정하므로 클래스도 함께 확인합니다(13.1).</li>
      <li><b><code>titbox</code>가 무클래스 그룹의 직속 자식입니다.</b> 한 겹 더 감싸면 CSS가 찾지
      못해 카드가 생기지 않습니다(4.2).</li>
      <li><b>조회영역이 세로 스택입니다.</b> PC의 가로 배치를 복사해 오지 않았는지 확인합니다(8.1).</li>
    </ul>

    <h3>크기와 간격</h3>
    <ul class="pts">
      <li><b>최상위 자식 사이 간격은 10입니다</b> — 타이틀바 · 조회영역 · 카드 · 하단 버튼(5.3).</li>
      <li><b>카드 안 간격이 10이 아닙니다 — 16입니다.</b> 오토레이아웃을 새로 만들면 간격 기본값이
      10이라, 카드 안에 10이 남아 있으면 값을 넣지 않은 것입니다(5.3).</li>
      <li><b>블록끼리는 32, 뒤따르는 부속은 8입니다</b> — 그리드·표·탭끼리는 32, 페이징과 하단
      버튼은 8입니다(5.3).</li>
      <li><b>좌우 여백 20을 이중으로 주지 않았습니다.</b> 인셋은 카드가 담당하며 카드 안 블록의
      좌우 여백은 0입니다(5.2).</li>
      <li><b>폭은 채우고 높이는 내용에 맡깁니다.</b> 폼 필드 · 조회 버튼 · 하단 버튼 · 그리드 셀은
      채우기, 아이콘 · 체크 상자처럼 크기가 정해진 요소는 고정 + 안쪽 여백 0입니다(5.4).</li>
      <li><b>배치 방향을 바꾼 컨테이너에 고정 높이가 남아 있지 않습니다.</b> 가로를 세로로 바꾸면서
      옛 고정 높이가 남아 30px로 눌린 사고가 있었습니다(5.4 · 6.3).</li>
    </ul>

    <h3>색과 선</h3>
    <ul class="pts">
      <li><b>투명이어야 할 컨테이너에 흰 채움이 남아 있지 않습니다.</b> 단, 카드와
      <code>schbox</code>의 배경은 원래 흰색이므로 그대로 둡니다(4.3).</li>
      <li><b>그리드 보더 모델이 맞습니다</b> — 셀은 우측선만, 행은 하단선만, 각 행의 마지막 셀은
      선이 없습니다. 바깥 테두리는 <code>gvwbox</code>가 담당합니다(11.1).</li>
      <li><b>모서리 값이 맞습니다</b> — 폼 필드 8 · 그리드 셀 0 · 건수 칩 4 · 달력 16(15.1 · 11.1 · 7.2 · 16.2).</li>
    </ul>

    <h2>19.2 자주 놓치는 것</h2>
    <p class="lead">정본이 <b>가장 자주 빠뜨리는 다섯 가지</b>로 꼽은 항목입니다. 검수 시간이 짧다면
    이 다섯 개만이라도 봅니다.</p>
    <div class="tblwrap">
      <table>
        <thead>
          <tr><th>놓치는 것</th><th>왜 안 보이나</th><th>참조</th></tr>
        </thead>
        <tbody>
          <tr><td><b>375 초과</b></td>
          <td>최상위 프레임이 내용을 잘라내 넘친 부분이 화면에서 사라집니다</td><td>5.1</td></tr>
          <tr><td><b>PC 값으로 풀린 변수모드</b></td>
          <td>「조금 작게 나왔네」 정도로만 보여 원인을 찾는 데 오래 걸립니다</td><td>1.3</td></tr>
          <tr><td><b>카드 안에 남은 간격 10</b></td>
          <td>최상위 간격이 실제로 10이라 값이 맞아 보입니다</td><td>5.3</td></tr>
          <tr><td><b>아이콘 오배치</b></td>
          <td>복제로 만든 버튼에 앞 버튼의 아이콘이 남습니다</td><td>13.1</td></tr>
          <tr><td><b>컴포넌트 기본값 텍스트</b></td>
          <td>글자가 채워져 있어 빈칸으로 보이지 않습니다</td><td>7.2</td></tr>
        </tbody>
      </table>
    </div>
    <div class="warn"><b>Figma에서 확인할 수 없는 것이 있습니다.</b> 어댑티브 표는 토큰이 있든 없든
    모바일 화면이 같고, 버튼과 트리거는 화면에서 구분되지 않습니다. 이 두 가지는 <b>결과 XML</b>에서
    확인합니다(10.2 · 13.2).</div>
    <div class="note"><b>값이 정본 문서와 달라 보일 때가 있습니다.</b> 버튼 높이 · 모서리 · 글자,
    그리드 행 높이, 탭의 모양, 우측 버튼 줄바꿈 규칙은 <b>정본 문서의 일부 절이 갱신 전 값으로 남아
    있습니다.</b> 어긋난 자리와 현행 값은 <b>21.1</b>에 정리해 두었습니다.</div>
  `,
  symptoms: `
    <p class="lead">결과가 의도와 다를 때 <b>증상에서 원인을 되짚는</b> 표입니다. 모바일에서 자주
    올라오는 신고를 원인별로 묶었습니다. <b>일부는 고치는 대상이 아니라 정본 동작</b>이므로,
    「조치 없음」이라고 적힌 줄은 그대로 두는 것이 맞습니다.</p>

    <h2>20.1 증상으로 찾기</h2>

    <h3>배치가 무너진다</h3>
    <div class="tblwrap">
      <table>
        <thead><tr><th>증상</th><th>원인</th><th>고치는 법</th><th>참조</th></tr></thead>
        <tbody>
          <tr><td>한 줄에 필드가 4개 이상 늘어선다</td>
          <td>나열 컨테이너의 한 줄 정원은 <b>3개</b>이며 4개째부터 다음 줄로 내려갑니다</td>
          <td>Figma에서도 한 줄에 필드를 3개까지만 그립니다. 마지막 줄은 남는 폭을 나눠 가지므로
          5개는 3 + 2로 떨어집니다</td><td>15.1</td></tr>
          <tr><td>라벨이 필드 위로 겹친다</td>
          <td>공백 없는 한 단어 라벨이 고정 폭 상자보다 길어 상자 밖에 그려집니다</td>
          <td>라벨을 짧게 쓰거나 띄어 씁니다. 현재 CSS는 상자 안에서 두 줄로 접어 겹침을 없앴습니다</td>
          <td>15.1</td></tr>
          <tr><td>어댑티브 표에서 라벨과 입력 짝이 깨진다</td>
          <td>행에 <code>w2tb_tr</code>이 없어 라벨 셀과 값 셀이 각각 줄바꿈됩니다</td>
          <td>모든 행 이름에 <code>w2tb_tr</code>을 적습니다. 자동 보강에 의존하지 않습니다</td>
          <td>3.2 · 10.1</td></tr>
          <tr><td>카드가 생기지 않고 내용이 배경에 붙는다</td>
          <td><code>titbox</code>가 무클래스 그룹의 직속 자식이 아닙니다</td>
          <td>제목과 내용을 클래스 없는 그룹 하나로 묶고, 제목을 한 겹 더 감싸지 않습니다</td>
          <td>4.1 · 4.2</td></tr>
          <tr><td>카드 안 내용이 다른 블록과 줄이 안 맞는다</td>
          <td>카드가 담당하는 좌우 인셋 20을 안쪽 블록에도 줘서 콘텐츠가 295로 줄었습니다</td>
          <td>카드 안 블록의 좌우 여백을 0으로 되돌립니다</td><td>5.2</td></tr>
          <tr><td>세로로 바꾼 영역이 납작하게 눌린다</td>
          <td>가로 배치 시절의 고정 높이가 안쪽 컨테이너에 남아 있습니다</td>
          <td>바깥부터 안쪽까지 높이를 「내용에 맞춤」으로 풉니다</td><td>5.4 · 6.3</td></tr>
        </tbody>
      </table>
    </div>

    <h3>크기·모양이 다르다</h3>
    <div class="tblwrap">
      <table>
        <thead><tr><th>증상</th><th>원인</th><th>고치는 법</th><th>참조</th></tr></thead>
        <tbody>
          <tr><td>버튼 크기가 제멋대로다</td>
          <td>모바일 버튼은 <b>배치</b>가 규격을 정합니다. 버튼을 죽 나열하는 컨테이너가 한때
          「버튼만 담은 그룹」으로 잡혀 크기가 뒤집힌 사고도 있었습니다</td>
          <td>높이는 어느 배치든 48입니다. 더 큰 버튼이 필요하면 <code>md</code>(52) ·
          <code>lg</code>(56)를 이름에 적습니다 — 클래스가 배치보다 우선합니다</td><td>13.1</td></tr>
          <tr><td>폼 필드가 작고 모서리가 각지다</td>
          <td>Mobile 변수모드 밖에서 컴포넌트를 꺼내 PC 값(24 · 2 · 12)으로 풀렸습니다</td>
          <td>디자인 템플릿의 모바일 섹션 안에서 작업합니다</td><td>1.3 · 15.1</td></tr>
          <tr><td>아이콘 버튼이 빈 사각형으로 나온다</td>
          <td>정해진 아이콘 클래스 목록에 없는 버튼에 <code>icon</code>만 붙였습니다</td>
          <td>목록에 있는 아이콘 클래스를 주거나, 텍스트 버튼으로 남깁니다</td><td>7.2 · 13.1</td></tr>
          <tr><td>기본탭과 서브탭이 예상과 반대로 보인다</td>
          <td>현행 모바일은 <b>기본탭 = 파란 칩 · 서브탭 = 검정 밑줄</b>입니다. 「기본탭 = 밑줄」은
          옛 규칙입니다</td>
          <td>Figma의 모양도 현행에 맞춰 그립니다. 클래스만 바꾸면 화면과 어긋납니다</td>
          <td>9.1 · 9.2</td></tr>
          <tr><td>토글이 체크박스로 나온다</td>
          <td>변환기가 체크박스 분기에서 노드명의 클래스를 통째로 버려
          <code>input_switch</code>가 결과에 실리지 않았습니다</td>
          <td>디자인 문제가 아닙니다. 최신 변환기에서 고쳐졌으므로 <b>변환기 판본</b>을 확인합니다</td>
          <td>15.2</td></tr>
          <tr><td>비활성 토글이 켜진 것처럼 파랗다</td>
          <td>실서버에서 다른 스타일시트가 뒤에 얹혀 「켜짐 = 파랑」 규칙이 이긴 사례가 있습니다</td>
          <td>Figma 쪽 문제가 아닙니다. 화면 담당자에게 스타일시트 적용 순서를 확인합니다</td>
          <td>15.2</td></tr>
          <tr><td>그리드 헤더 글자가 잘린다</td>
          <td>화면 열 폭은 CSS가 4단계(44 · 88 · 152 · 채우기)로 다시 정합니다</td>
          <td>줄바꿈으로 풀지 않습니다 — 한 줄 유지가 정본이며, 실무 화면(3~5열)에서는 잘리지
          않습니다</td><td>11.1</td></tr>
        </tbody>
      </table>
    </div>

    <h3>빠지거나 다른 것으로 나온다</h3>
    <div class="tblwrap">
      <table>
        <thead><tr><th>증상</th><th>원인</th><th>고치는 법</th><th>참조</th></tr></thead>
        <tbody>
          <tr><td>그리드 데이터가 통째로 비어 있다</td>
          <td>데이터 행 이름에 <code>row</code>가 없어 행으로 인식되지 않았습니다. 경고도 남지
          않습니다</td>
          <td>행 이름에 <code>row</code>를 넣습니다. 하위 이름에 <code>noresult</code>가 섞여
          있지 않은지도 함께 봅니다</td><td>11.1 · 11.5</td></tr>
          <tr><td>트리 노드가 하위까지 함께 사라진다</td>
          <td>두 번째 조각이 <code>node</code>와 완전히 일치하지 않습니다</td>
          <td>노드 프레임의 두 번째 조각을 <code>node</code>로 맞춥니다</td><td>12.1</td></tr>
          <tr><td>아코디언 본문의 표·버튼이 사라진다</td>
          <td>패널 본문은 첫 텍스트 한 줄만 전달됩니다</td>
          <td>본문이 복잡한 화면에는 아코디언 대신 탭을 씁니다</td><td>14.1</td></tr>
          <tr><td>목록의 불릿이 사라지고 한 덩어리로 보인다</td>
          <td><code>listbox</code> 클래스가 없어 목록 태그가 붙지 않았습니다</td>
          <td>목록 그룹에 <code>listbox</code>를 주고, 항목을 그 직속 하위에 둡니다</td><td>18.1</td></tr>
          <tr><td>레이어가 통째로 결과에 없다</td>
          <td>Figma에서 표시를 끈 숨김 레이어입니다. 상위를 숨기면 하위도 함께 빠집니다</td>
          <td>전달 전 레이어 패널에서 표시 상태를 확인합니다</td><td>4.4</td></tr>
          <tr><td>날짜 입력칸·펼친 달력이 그냥 그룹으로 나온다</td>
          <td>배포본 변환기에 해당 분기가 아직 없는 판본입니다</td>
          <td>디자인이 아니라 <b>변환기 판본</b>을 먼저 확인합니다</td><td>16.1 · 16.2</td></tr>
        </tbody>
      </table>
    </div>

    <h3>정본 동작이라 고치지 않는 것</h3>
    <div class="tblwrap">
      <table>
        <thead><tr><th>증상</th><th>원인</th><th>고치는 법</th><th>참조</th></tr></thead>
        <tbody>
          <tr><td>드롭다운 메뉴가 엉뚱한 곳에 뜬다</td>
          <td>그 컨트롤은 <b>단말 기본 셀렉트</b>라 메뉴를 브라우저와 운영체제가 그립니다.
          CSS로도 스크립트로도 위치·모양을 바꿀 수 없습니다</td>
          <td><b>조치 없음.</b> 실제 휴대폰에서는 같은 컨트롤이 운영체제 기본 피커로 떠서 정상으로
          보입니다 — 데스크탑 브라우저로 볼 때만 어색합니다</td><td>15.1</td></tr>
          <tr><td>캘린더 팝업이 화면 밖으로 나간다</td>
          <td>팝업은 화면 최상위에 떠 있고, 엔진이 입력칸의 좌측 좌표를 그대로 박아 폭 335가
          오른쪽으로 밀려났습니다</td>
          <td>가로는 CSS가 화면 한가운데(좌 20 / 우 355)로 고정해 해결했습니다. 이미 조치된
          항목이므로 디자인에서 손댈 것이 없습니다</td><td>16.1</td></tr>
          <tr><td>화면 아래쪽 날짜칸을 누르면 달력이 위로 뜬다</td>
          <td>세로 위치는 엔진이 「아래 공간이 부족하면 위로」 계산해 직접 박습니다</td>
          <td><b>조치 없음</b> — 결함이 아니며 현 상태로 두기로 결정된 항목입니다</td><td>16.1</td></tr>
          <tr><td>요일이 영문으로 나온다</td>
          <td>요일 칸 마크업 판본이 달라 한글로 바꾸는 규칙이 걸리지 않은 상태입니다</td>
          <td>모바일의 정답은 한글(일~토)입니다. 현재 CSS는 이 판본까지 처리하므로,
          영문으로 보이면 <b>모바일 CSS가 적용되지 않은 화면</b>을 의심합니다</td><td>16.2</td></tr>
          <tr><td>주말만 색이 다르다</td>
          <td>PC 달력을 그대로 옮겼습니다. 모바일 정본에는 주말 색 구분이 없습니다</td>
          <td>날짜를 전부 같은 색으로 그립니다. 전월·익월만 흐리게 둡니다</td><td>16.2</td></tr>
          <tr><td>브레드크럼이 사라졌다</td>
          <td>모바일 타이틀바는 [타이틀, 아이콘] 한 줄이며 경로는 규칙상 제외입니다</td>
          <td><b>조치 없음.</b> 빠졌다고 다시 그려 넣지 않습니다</td><td>7.1</td></tr>
          <tr><td>그리드가 가로로 스크롤된다</td>
          <td>컬럼 수만큼 넓어진 표를 스크롤로 받는 것이 모바일 정본입니다</td>
          <td><b>조치 없음.</b> 열 균등분할로 없애려다 글자가 잘려 되돌린 이력이 있습니다</td>
          <td>11.1</td></tr>
          <tr><td>어댑티브 표인지 화면에서 구분되지 않는다</td>
          <td>모바일 CSS가 이미 모든 표를 세로로 쌓아 토큰 유무가 화면에 나타나지 않습니다</td>
          <td>검수는 결과 XML에서 합니다. 확인이 안 된다고 토큰을 빼지 않습니다</td><td>10.2</td></tr>
        </tbody>
      </table>
    </div>
  `,
  appendix: `
    <p class="lead">참조용 장입니다. PC와의 차이 · 클래스 목록 · 컴포넌트 카탈로그 · 확인 도구 ·
    컴포넌트 이름 전체표를 한자리에 모았습니다.</p>

    <h2>21.1 PC ↔ 모바일 한눈에</h2>
    <p>모바일 화면을 만들 때 <b>PC와 값이 달라지는 자리</b>입니다. 여기에 없는 항목은 PC와 같습니다.</p>
    <div class="tblwrap">
      <table>
        <thead><tr><th>항목</th><th>PC</th><th>모바일</th><th>참조</th></tr></thead>
        <tbody>
          <tr><td>화면 폭 / 좌우 여백</td><td>가변</td>
          <td><b>375 / 20 → 콘텐츠 335</b></td><td>5.1</td></tr>
          <tr><td>블록 간격</td><td>정본에 PC 값이 없습니다</td>
          <td><b>최상위 10 · 카드 안 16 · 블록끼리 32 · 부속 8</b></td><td>5.3</td></tr>
          <tr><td>프라이머리 색</td><td><code>#256EF4</code></td>
          <td><b><code>#237AF3</code></b></td><td>5.1</td></tr>
          <tr><td>폼 필드 높이 / 모서리 / 글자</td><td>24 / 2 / 12</td>
          <td><b>48 / 8 / 16</b></td><td>15.1</td></tr>
          <tr><td>폼 필드 폭</td><td>XML의 폭 값</td>
          <td><b>335 (한 줄 전체)</b></td><td>5.4 · 15.1</td></tr>
          <tr><td>폼 테두리(기본)</td><td><code>#C4CDD5</code></td>
          <td><b><code>#DDDDDD</code></b></td><td>15.1</td></tr>
          <tr><td>본문 글자</td><td>12</td>
          <td>정본은 <b>14</b>이나, 현재 CSS의 「모든 텍스트는 16」 규칙이 뒤에서 덮어
          <b>화면에는 16</b>으로 나옵니다</td><td>7.2 · 10.1</td></tr>
          <tr><td><b>★</b> 버튼 높이</td><td>24</td>
          <td><b>기본 48 · <code>md</code> 52 · <code>lg</code> 56</b></td><td>13.1</td></tr>
          <tr><td><b>★</b> 버튼 모서리 / 글자</td><td>4 / 12</td>
          <td><b>같은 줄에 얹히면 6 · 자기 행을 통째로 차지하면 8</b>(<code>md</code>·<code>lg</code>·아이콘
          전용도 8) <b>/ 16</b> — 판정 기준은 13.1 하나입니다</td><td>13.1</td></tr>
          <tr><td><b>★</b> 버튼 크기를 정하는 것</td><td>클래스</td>
          <td><b>배치</b>가 여백·모서리·아이콘 크기를 정합니다. <b>높이는 어느 배치든 48</b>이며,
          클래스를 적으면 배치보다 우선합니다</td><td>13.1</td></tr>
          <tr><td><b>★</b> 아이콘 전용 버튼</td><td>정본에 PC 값이 없습니다</td>
          <td><b>48 × 48 · 모서리 8 · 아이콘 20</b></td><td>13.1</td></tr>
          <tr><td>체크 / 라디오</td><td>16 · 테두리 <code>#C4CDD5</code></td>
          <td><b>20 · 테두리 <code>#E6E8EA</code></b></td><td>15.1</td></tr>
          <tr><td><code>schbox</code> 구조</td><td>가로 한 줄(라벨·필드·라벨·필드) · 버튼 우측</td>
          <td><b>세로 스택</b>(라벨 위 · 필드 아래) · 버튼 폭 100%</td><td>8.1</td></tr>
          <tr><td>입출력 표 배치</td><td>라벨 왼쪽 · 값 오른쪽</td>
          <td><b>라벨 위 · 값 아래</b>(어댑티브 여부와 무관하게 항상)</td><td>10.1 · 10.2</td></tr>
          <tr><td><code>req</code> 표시</td><td>파란 점 ●</td>
          <td><b>별표 <code>*</code></b></td><td>8.1 · 10.2</td></tr>
          <tr><td><b>★</b> 그리드 행 높이</td><td>30</td><td><b>42</b></td><td>11.1</td></tr>
          <tr><td><b>★</b> 그리드 셀 글자</td><td>정본에 PC 값이 없습니다</td>
          <td><b>16 Medium</b></td><td>11.1</td></tr>
          <tr><td>그리드 컬럼 폭</td><td>열 정의 / 자동 맞춤</td>
          <td>Figma는 <b>90 고정</b>, 화면은 CSS가 <b>4단계(44 · 88 · 152 · 채우기)</b>로 다시 정하고
          넘치는 만큼은 가로 스크롤</td><td>11.1</td></tr>
          <tr><td>그리드 바디 보더</td><td><code>#C4CDD5</code> 점선</td>
          <td><b><code>#EAEEF1</code> 실선</b></td><td>11.1</td></tr>
          <tr><td>영역 컨테이너</td><td>전용 컨테이너 클래스를 붙인 그룹</td>
          <td><b>클래스 없는 <code>group</code></b> — CSS가 <code>titbox</code>를 직속으로 품은
          그룹을 찾아 카드로 그립니다</td><td>4.1</td></tr>
          <tr><td><code>pgtbox</code> 구성</td><td>[타이틀, 경로, 즐겨찾기]</td>
          <td><b>[타이틀, 아이콘]</b> — 경로(브레드크럼) 제외</td><td>7.1</td></tr>
          <tr><td><b>★</b> <code>titbox</code> 우측 버튼</td><td>텍스트 버튼</td>
          <td><b>아이콘 전용 48 × 48</b> — 정해진 아이콘 클래스 목록에 있는 것만</td><td>7.2</td></tr>
          <tr><td><b>★</b> 우측 버튼 줄바꿈</td><td>—</td>
          <td><b>넘칠 때만 개행</b>합니다 — 개수로 미리 두 줄을 만들지 않습니다</td><td>7.2</td></tr>
          <tr><td><b>★</b> 탭</td><td>회색 띠 + 파란 칩</td>
          <td><b>기본탭 = 파란 칩 · 서브탭 = 검정 밑줄</b></td><td>9.1 · 9.2</td></tr>
          <tr><td>달력 모서리</td><td>정본에 PC 값이 없습니다</td>
          <td><b>16</b> — CSS에 적힌 4가 아닙니다</td><td>16.2</td></tr>
          <tr><td>달력 주말</td><td>일요일 빨강 · 토요일 파랑</td>
          <td><b>색 구분 없음</b>(날짜 전부 <code>#333333</code>)</td><td>16.2</td></tr>
          <tr><td><b>★</b> 토글 스위치</td><td>40 × 20 · 노브 14</td>
          <td><b>52 × 28 · 노브 22</b></td><td>15.2</td></tr>
        </tbody>
      </table>
    </div>
    <div class="warn"><b>★ 표시한 줄은 정본 문서의 해당 절이 갱신 전 값으로 남아 있는
    자리입니다.</b> 디자인 템플릿을 다시 재고 사용자 지시로 값이 바뀐 뒤 일부 절이 함께 고쳐지지
    않았습니다 — 버튼 높이·모서리·글자, 배치에 따른 승격, 아이콘 전용 버튼 크기, 그리드 행 높이와
    셀 글자, <code>titbox</code> 우측 버튼의 아이콘 전용 전환, 우측 버튼 줄바꿈 조건,
    탭 두 종류의 역할, 토글 스위치 치수가 해당합니다(★ 열 개).
    <b>이 가이드와 컴포넌트별 챕터(6~18장)는 화면에 실제로 나오는 현행 값을 기준으로 적었습니다.</b>
    정본 문서와 다른 값을 보면 컴포넌트별 챕터를 기준으로 삼습니다.</div>

    <h2>21.2 클래스 목록</h2>
    <p>레이어 이름의 <b>두 번째 밑줄 뒤</b>에 쓰는 값입니다(2.1). 여러 개면 공백으로 구분합니다(2.2).
    <b>클래스 어휘는 PC와 모바일이 같습니다</b> — 같은 이름에 모바일 값이 연결되어 있을 뿐입니다(3.3).</p>
    {{CLASS_TABLE}}
    <div class="note">본 표는 <b>디자인 템플릿에서 실제 사용 중인</b> 클래스 목록입니다.
    컴포넌트별 챕터(6~18장)에서 안내하는 클래스는 표에 없어도 사용할 수 있습니다.
    <b>표와 챕터 어디에도 없는 이름은 변환기가 인식하지 않습니다.</b></div>
    <div class="warn"><b>영역을 감싸는 그룹에는 이 표의 컨테이너 클래스를 주지 않습니다.</b>
    모바일의 카드는 클래스가 아니라 구조로 성립합니다(4.1).</div>

    <h2>21.3 컴포넌트 카탈로그</h2>
    <p>모바일 컴포넌트 세트와 그 위치입니다. 화면을 만들 때는 <b>디자인 템플릿에 이미 놓여 있는
    인스턴스를 복제하는 것이 우선</b>이고, 없을 때 이 목록에서 꺼냅니다.</p>
    <div class="tblwrap">
      <table>
        <thead><tr><th>컴포넌트 세트</th><th>id</th><th>비고</th></tr></thead>
        <tbody>
          <tr><td><code>Form/Input</code></td><td>15382:2452</td><td>상태 8종</td></tr>
          <tr><td><code>Form/Select</code></td><td>15382:2471</td><td>상태 8종 · 우측 꺽쇠</td></tr>
          <tr><td><code>Form/Searchbox</code></td><td>15382:2514</td><td></td></tr>
          <tr><td><code>Form/Textarea</code></td><td>15382:2545</td><td></td></tr>
          <tr><td><code>Form/Secret</code></td><td>15537:6790</td><td>입력칸 복제</td></tr>
          <tr><td><code>Form/AutoComplete</code></td><td>15537:6834</td><td>셀렉트 복제</td></tr>
          <tr><td><code>Form/CheckCombobox</code></td><td>15537:6875</td><td>셀렉트 복제</td></tr>
          <tr><td><code>Form/Spinner</code></td><td>15382:2555</td><td></td></tr>
          <tr><td><code>Form/MultiSelect</code></td><td>15382:2648</td><td></td></tr>
          <tr><td><code>Form/Upload</code></td><td>15382:2667</td><td></td></tr>
          <tr><td><code>Form/InputCalendar</code></td><td>15382:2698</td><td>날짜 입력칸(16.1)</td></tr>
          <tr><td><code>Form/CheckMark</code></td><td>15382:2781</td><td>체크박스 마커</td></tr>
          <tr><td><code>Form/RadioMark</code></td><td>15382:2787</td><td>라디오 마커</td></tr>
          <tr><td><code>Form/ToggleSwitch</code></td><td>15537:6539</td><td>상태 4종(15.2)</td></tr>
          <tr><td><code>Form/FlipToggle</code></td><td>15382:2818</td><td></td></tr>
          <tr><td><code>Form/ProgressBar</code></td><td>15397:42260</td><td></td></tr>
          <tr><td><code>slider</code></td><td>15397:42243</td><td></td></tr>
          <tr><td><code>Button/Text</code></td><td>15382:2733</td><td>크기 3종(13.1)</td></tr>
          <tr><td><code>Button/Icon</code></td><td>15382:2774</td><td>아이콘 전용</td></tr>
          <tr><td><code>titbox</code></td><td>15382:5646</td><td>건수 칩·아이콘 버튼 포함(7.2)</td></tr>
          <tr><td><code>count_group_total_count</code></td><td>15382:2878</td><td>건수 칩</td></tr>
          <tr><td><code>Tab/Item</code></td><td>15382:2793</td><td>탭 하나(9.1)</td></tr>
          <tr><td><code>Grid/HeaderCell</code></td><td>15382:2906</td><td>마스터에 채움·선이 없습니다</td></tr>
          <tr><td><code>Grid/BodyCell</code></td><td>15382:2896</td><td>마스터에 채움·선이 없습니다</td></tr>
          <tr><td><code>Paging/Num</code></td><td>15382:2847</td><td>페이지 목록(11.5)</td></tr>
          <tr><td><code>nrow</code></td><td>15382:2865</td><td>트리 행(12.1)</td></tr>
          <tr><td><code>Mobile/Accordion/Panel</code></td><td>15396:38442</td><td>아코디언 패널(14.1)</td></tr>
          <tr><td><code>Mobile/Calendar</code></td><td>15397:41496</td><td>연·월 / 연·월·일 두 변형 · 모서리 16</td></tr>
          <tr><td><code>Mobile/date</code></td><td>15397:42120</td><td>날짜 칸 · 주말 색 구분 없음</td></tr>
        </tbody>
      </table>
    </div>
    <div class="note"><b>세트가 아닌 단일 컴포넌트</b> — 메시지 박스
    <code>msg_group_msgbox</code> 5종(15537:6517~6521)과 목록 <code>list_group_listbox</code>
    계열(15537:6522~6526)입니다(18.1). 화면 타이틀바 마스터는 <code>pgtbox</code> 15382:5626입니다(7.1).</div>
    <div class="warn"><b>변형이 있는 컴포넌트는 복제하지 말고 각각 그립니다.</b>
    인스턴스를 자기 컴포넌트로 다시 교체하면 <b>텍스트 오버라이드가 마스터 기본값으로 초기화</b>되므로,
    폭이 틀어진 인스턴스를 정상화한 직후에는 텍스트를 반드시 다시 넣습니다.</div>

    <h2>21.4 확인 도구</h2>
    <p>화면을 캡처해 눈으로 대조하는 대신 <b>명령으로 확인</b>합니다. 실행은 저장소 루트에서 합니다.</p>
    <div class="tblwrap">
      <table>
        <thead><tr><th>명령</th><th>확인하는 것</th></tr></thead>
        <tbody>
          <tr><td><code>node tools/css-verify/verify.mjs</code></td>
          <td><b>모바일 CSS 잠금 검사</b> — 스크린샷 대신 이것을 씁니다. 통과 개수는 잠금이 늘어날수록
          함께 늘어납니다(정본 기준 135, 2026-08-06 기준 158)</td></tr>
          <tr><td><code>node_modules/.bin/tsx css/xml-verify/namecheck.ts "이름1" "이름2"</code></td>
          <td>레이어 이름 하나가 어떤 컴포넌트·클래스로 매핑되는지</td></tr>
          <tr><td><code>node_modules/.bin/tsx css/xml-verify/namecheck-bulk.ts &lt;json&gt;</code></td>
          <td>이름 여러 개를 한 번에 검사</td></tr>
          <tr><td><code>node_modules/.bin/tsx css/xml-verify/e2e-x7.ts</code></td>
          <td>이름 → 자동매핑 → 변환까지 이어지는 전체 흐름</td></tr>
        </tbody>
      </table>
    </div>
    <div class="warn"><b>Figma에서 이름만 바꾸면 변환 결과가 바뀌지 않습니다.</b>
    매핑은 레이어 식별자 기준으로 저장되어 있으므로, 이름을 고쳤다면 <b>다시 가져오기 → 자동매핑
    재실행</b>까지 해야 결과에 반영됩니다.</div>
    <div class="note"><b>모바일 CSS를 고쳤다면 개인설정의 등록본도 함께 갱신합니다.</b>
    등록본이 제품에 들어 있는 CSS를 이기기 때문에, 낡은 등록본이 남아 있으면 최신 CSS가 영원히
    반영되지 않습니다 — 오류도 남지 않습니다.</div>

    <h2>21.5 컴포넌트 이름 전체표</h2>
    <p>레이어 이름의 <b>두 번째 조각</b>에 쓰는 값입니다(2.1). 변환기가 컴포넌트 유형을 판정하는
    <b>유일한 키</b>이며, <b>이 표에 없는 값은 인식되지 않습니다.</b>
    <b>이름 어휘는 PC와 모바일이 같습니다</b> — 달라지는 것은 그 컴포넌트가 375에서 어떤 치수·배치로
    그려지는가뿐입니다.</p>
    {{REGISTRY_NAMES}}
    <div class="note"><b>세 번째 조각(클래스 자리)의 값은 21.2에 있습니다.</b>
    단 <code>calendar</code> · <code>slider</code> 계열만은 세 번째 조각이 클래스가 아니라
    <b>변형 지정자</b>입니다(2.5 · 16.2).</div>

    <h3>아이콘 클래스 목록</h3>
    <p>버튼·트리거의 아이콘 그림을 정하는 값입니다. 이름의 클래스 자리에 <code>btn_cm</code>과 함께
    적습니다(13.1). <b>아이콘 전용 버튼은 여기에 <code>icon</code>을 더하고 텍스트 레이어를 두지
    않습니다.</b></p>
    <p><code>copy</code> <code>row_add</code> <code>row_del</code> <code>upload</code>
    <code>download</code> <code>search</code> <code>left</code> <code>right</code> <code>up</code>
    <code>down</code> <code>delete</code> <code>calendar</code> <code>guide</code> <code>refresh</code>
    <code>save</code> <code>fav</code> <code>link</code> <code>print</code></p>
    <div class="warn"><b>이 목록에 없는 버튼(예 「취소」)에 <code>icon</code>만 붙이면 빈 사각형이
    됩니다.</b> 아이콘 클래스를 주거나 텍스트 버튼으로 남깁니다(7.2 · 13.1).</div>
  `,
};

/* ─────────────────────────────────────────────────────────────────────
   모바일판 모드 플래그.
   assets/app.js 는 PC판과 한 벌을 공유하므로, 모바일에서만 달라져야 하는
   문구(치수 각주 · 완성 모습 안내)는 이 값으로 분기한다.
   이 파일은 index-mobile.html 에서 app.js 보다 **먼저** 로드된다.
   ───────────────────────────────────────────────────────────────────── */
window.GUIDE_MOBILE = true;

/* ─────────────────────────────────────────────────────────────────────
   카탈로그 장(6~18)의 도입부 — **모바일용 한 벌**.
   app.js 안의 CHAPTER_INTRO 는 PC 문장이라 그대로 쓰면 아래 항목과 어긋난다
   (어댑티브가 화면에 나타나지 않음 · 브레드크럼 없음 · 행 클래스 직접 기재 · 절 번호 불일치).
   app.js 는 window.GUIDE_CHAPTER_INTRO 가 있으면 그것을 쓰므로,
   PC 페이지(index.html)는 이 파일을 읽지 않아 원래 문장을 그대로 유지한다.
   ※ 괄호 안 절 번호는 **모바일에 실재하는 절**만 쓴다. 없는 번호는 링크가 되지 않는다.
   ───────────────────────────────────────────────────────────────────── */
window.GUIDE_CHAPTER_INTRO = {
  layout: `<p>레이아웃은 화면의 한 영역을 여러 칸으로 나누는 컨테이너입니다. 그룹 레이어 이름의
  클래스 자리에 <code>lybox</code>를 지정하면 나뉘며, <b>이름 규칙은 PC와 완전히 동일합니다.</b>
  네 항목 중 앞의 둘(분할 · 분할(고정))과 뒤의 둘(셔틀)은 같은 <code>lybox</code> 구조이고,
  셔틀에만 이동 버튼 줄(<code>ly_btn</code>)이 한 겹 더 있습니다.</p>
  <div class="warn"><b>모바일에서는 방향 클래스가 결과를 반대로 뒤집습니다.</b>
  방향을 붙이지 않은 <code>lybox</code>는 <b>375에서도 가로 2단 그대로</b>이고(6.1 · 6.2),
  <code>horizontal</code>·<code>vertical</code>을 붙인 셔틀은 <b>세로 스택으로 쌓입니다</b>(6.3 · 6.4).
  이름과 보이는 방향이 어긋나 보여도 <b>클래스는 정본 XML 그대로 둡니다</b> — 바뀐 것은 CSS가 그리는
  모양뿐입니다. <b>Figma는 언제나 렌더 결과대로</b> 그립니다.</div>
  <div class="note"><b>패널 폭은 이름의 클래스가 결정합니다.</b> 클래스가 없는 패널은 균등 분할이며,
  비율은 <code>col_2</code>~<code>col_9</code>, px 고정폭은 <code>col_fix</code>입니다.
  Figma에서 폭만 다르게 그려도 <code>col_fix</code>가 아니면 결과에 실리지 않습니다.
  <b>고정폭은 375 기준으로 다시 잡습니다</b> — PC에서 쓰던 값을 그대로 옮기면 반대편 패널이 사실상
  사라집니다(6.2).</div>
  <div class="note"><b>패널이 <code>titbox</code>를 직속으로 품으면 카드가 됩니다.</b>
  패널에 클래스를 주지 않아도 구조만으로 흰 배경과 안쪽 여백이 붙습니다(4.1 · 4.3).
  Figma에도 패널을 카드 모양으로 그려 결과와 맞춥니다.</div>`,

  title: `<p>본 장은 화면 최상단의 타이틀바(<code>pgtbox</code>)와 영역 제목(<code>titbox</code>)
  두 단계(<code>tit_main</code> · <code>tit_sub</code>)를 다룹니다. 뒤의 두 항목은
  <b>제목 앞의 막대·점 장식은 디자이너가 그리는 것이 아니라 클래스의 CSS가 만든다</b>는 규칙을
  공유합니다(<code>pgtbox</code>에는 이 장식이 없습니다). 장식 레이어는 Figma에 그리되, 그 형태를
  바꿔 결과를 조정할 수는 없습니다.</p>
  <div class="warn"><b>모바일 타이틀바에는 브레드크럼(이동 경로)을 만들지 않습니다.</b>
  <code>breadcrumb</code> 그룹과 그 안의 뎁스 항목·구분자를 <b>모두 그리지 않으며</b>,
  <code>pgtbox</code>의 자식은 <b>[타이틀, 아이콘] 둘뿐</b>입니다. PC 화면을 옮길 때 경로가
  사라지는 것은 규칙대로이며 오류가 아닙니다(7.1 · 20.1).</div>
  <div class="note"><b>제목줄 우측 버튼은 아이콘 전용입니다.</b> 라벨 없이 아이콘만 그려지며
  <b>48 × 48 · 모서리 8 · 아이콘 20</b>입니다. 클래스에 <code>icon</code> 토큰을 넣고
  <b>텍스트 레이어를 두지 않습니다.</b> 아이콘 그림은 <code>save</code>·<code>fav</code> 같은
  아이콘 클래스가 정하며, 정해진 목록에 없는 버튼에 <code>icon</code>만 붙이면 빈 사각형이 됩니다
  (7.2 · 13.1).</div>
  <div class="note"><b>줄 높이는 최소 48입니다.</b> 우측 아이콘 버튼이 48로 올라가면서
  <code>titbox</code> 줄 높이도 함께 48이 되었습니다. 제목 글자는 정본 18이지만 현재 CSS의
  「모든 텍스트는 16」 규칙이 뒤에서 덮어 <b>화면에는 16</b>으로 나옵니다 — <b>Figma는 정본대로 18</b>로
  그립니다(7.2 · 21.1).</div>`,

  schbox: `<p>조회영역은 화면 상단에서 조회 조건을 입력받는 영역입니다. 표(<code>table</code> ·
  <code>tr</code> · <code>th</code> · <code>td</code>)와 폼 필드가 함께 들어가, <b>레이어 이름 · 클래스 ·
  구조 · 오토레이아웃 네 가지가 모두 적용되는</b> 컴포넌트입니다.</p>
  <div class="warn"><b>본 장은 PC와 구조 자체가 다른 몇 안 되는 자리입니다.</b>
  PC는 한 행에 <code>라벨 · 필드 · 라벨 · 필드</code>를 가로로 늘어놓고 조회 버튼을 오른쪽에 두지만,
  <b>모바일은 라벨이 필드 위로 올라가는 세로 스택이고 조회 버튼은 폭 100%</b>입니다(8.1).
  PC 조회영역을 복사해 값만 바꾸면 Figma와 화면이 통째로 어긋납니다.</div>
  <div class="warn"><b>모든 행 이름에 <code>w2tb_tr</code>을 직접 적습니다.</b>
  변환기가 행 클래스를 보강하기는 하지만 <b>모바일 정본은 직접 기재를 기준으로 삼습니다</b> —
  자동 보강에 의존하지 않습니다(3.2 · 4.4). 빠지면 라벨 셀과 값 셀이 각각 줄바꿈되어
  <b>[라벨 / 입력] 짝이 깨집니다.</b> 셀의 속성 블록과 표의 열 너비 정의는 변환기가 자동으로
  만들어 줍니다.</div>
  <div class="note"><b>어댑티브 토큰은 자동으로 붙지 않습니다.</b>
  <code>w2tb_adaptive_layout</code>은 <b>표 레이어 이름에 직접</b> 지정해야 하며, 누락해도 경고가
  나지 않습니다. 다만 <b>모바일에서는 이 토큰이 있든 없든 화면이 같습니다</b> — 모바일 CSS가 이미
  모든 표를 세로로 쌓기 때문입니다. <b>검수는 캔버스가 아니라 결과 XML에서 합니다</b>(8.2 · 10.2).</div>`,

  tab: `<p>탭은 한 영역을 여러 장으로 나눠 전환하는 컴포넌트입니다. 일곱 항목이 있으나
  <b>기본 구조는 하나</b>입니다 — <code>tbcbox</code> 그룹 › 탭 컨트롤(<code>tbc</code>) 밑에
  <code>tabhost</code>(그 안에 <code>tabs</code> 프레임)와 본문(<code>content</code>) 그룹이
  <b>형제로 나란히</b> 있습니다.
  나머지 여섯은 이 구조에서 <b>한 지점만</b> 바꾼 형태이므로, 9.1을 먼저 보고 나머지는 차이만 봅니다.
  <b>이름 규칙은 PC와 동일합니다.</b></p>
  <div class="note"><b>변형은 탭 컨트롤이 아니라 상위 그룹 이름이 정합니다.</b>
  <code>tbcbox</code> 그룹 이름의 <b>첫 조각</b>이 <code>scroll</code>이면 스크롤,
  <code>left</code>·<code>right</code>·<code>bottom</code>이면 탭 위치, <code>adaptive</code>면
  아코디언입니다. 탭 컨트롤 레이어에 지정하면 걸리지 않습니다. 서브탭만 예외로 탭 컨트롤의
  <b>클래스</b>(<code>tbc_sub</code>)가 정합니다(9.2).</div>
  <div class="warn"><b><code>tabhost</code>가 있고 없고로 결과가 통째로 갈립니다.</b>
  탭을 탭 컨트롤 <b>직속</b>에 두면 변환기가 어댑티브(아코디언)로 판정하고, 그러면 탭 위치는
  적용되지 않습니다. 위치 변형(9.4~9.6)에는 <code>tabhost</code>가 필수이고 어댑티브(9.7)에는
  쓰지 않습니다.</div>
  <div class="warn"><b>375에서는 위치 변형이 대부분 살아남지 않습니다.</b>
  <code>left</code>·<code>right</code>는 세로 탭 열을 둘 자리가 없어 <b>위쪽 가로 탭 줄</b>로
  그려지고(9.4 · 9.5), 실제로 자리가 바뀌는 것은 <code>bottom</code>뿐입니다(9.6).
  어댑티브는 기준 폭이 1920이라 <b>375에서는 언제나 아코디언</b>입니다(9.7).
  <b>Figma도 렌더 결과대로</b> 그립니다.</div>
  <div class="note"><b>선택된 탭의 모양은 기본탭과 서브탭이 정반대입니다</b> —
  기본탭은 <b>파란 칩</b>, 서브탭은 <b>검정 밑줄</b>입니다. 「기본탭 = 밑줄」은 옛 규칙입니다(21.1).
  내부를 자체 방식으로 해석하는 컴포넌트 중에서 <b>탭 본문은 작성한 구조가 그대로 유지되는 유일한
  자리</b>입니다(4장 참조). 탭·본문의 <code>id</code>는 변환기가 부여합니다.</div>`,

  tblbox: `<p>입출력 테이블은 라벨과 값을 짝지어 보여주거나 입력받는 표입니다.
  <b>표 작성 규칙은 8장 조회영역과 같습니다</b> — 행은 두 번째 조각이 <code>tr</code>,
  라벨셀은 <code>th</code>, 값셀은 <code>td</code>입니다. 차이는 바깥 컨테이너
  (<code>tblbox</code> 한 겹)와, 조회영역과 달리 <code>schbox_inner</code>가 없다는 점입니다(10.1).</p>
  <div class="note"><b>모바일에서는 라벨이 값 위로 올라갑니다 — 어댑티브 여부와 무관하게 항상입니다.</b>
  라벨셀 폭을 조정해도 화면은 바뀌지 않습니다(열 폭이라는 개념이 없습니다). 다만 폭 값 자체는
  결과 XML의 열 너비 정의에 실리므로, 같은 화면을 PC로도 볼 계획이면 열마다 값을 맞춰 둡니다.
  <code>tblbox</code>는 화면 끝까지 늘어나지 않고 <b>카드 안쪽 폭 335</b>를 씁니다(5.2).</div>
  <div class="warn"><b>어댑티브(10.2)는 좁은 화면에서만 접히는 표가 아닙니다.</b>
  <code>w2tb_adaptive_layout</code>을 지정하면 <b>화면 폭과 무관하게 항상</b> 「라벨 + 값」 한 쌍씩
  한 줄로 배치됩니다. <b>다만 모바일 화면에서는 토큰이 있든 없든 결과가 같아 보입니다</b> —
  모바일 CSS가 이미 모든 표를 세로로 쌓기 때문입니다. <b>확인이 안 된다고 토큰을 빼지 않습니다</b> —
  같은 XML을 PC로 볼 때 배치가 달라집니다.</div>
  <div class="note">셀의 헤더 여부, 표의 열 너비 정의, 표 요약 블록은 <b>변환기가 자동으로
  만듭니다</b>(4.4). <b>행의 <code>w2tb_tr</code>은 예외입니다</b> — 자동 보강에 기대지 말고
  <b>모든 행에 직접 적습니다</b>(3.2).</div>`,

  grid: `<p>그리드는 다량의 데이터를 행과 열로 보여주는 컴포넌트입니다. 표(10장)와 비슷해 보이지만
  <b>구성 방식이 다릅니다</b> — 표는 셀을 하나씩 쓰지만, 그리드는 <b>헤더 행 하나가 컬럼 전체를
  정의하고</b> 나머지 행은 그 컬럼의 데이터가 됩니다. 다섯 항목 모두 <code>gvwbox</code> 그룹 ›
  그리드(<code>gvw</code>) › <code>header_row</code> + <code>body_row</code>의 같은 구조입니다.</p>
  <div class="warn"><b>판정 기준은 행 이름에 <code>header</code>·<code>row</code>·<code>footer</code>가
  들어 있는지입니다.</b> <code>header</code>가 든 행은 컬럼 정의, <code>row</code>가 들고
  <code>header</code>·<code>footer</code>가 없는 행은 데이터, <code>footer</code>가 든 행은 합계
  행입니다. <b>어느 조건에도 맞지 않는 행은 결과에서 빠집니다</b> — <code>line1</code>·<code>data2</code>
  처럼 지으면 데이터가 통째로 누락되고 오류도 경고도 남지 않습니다(11.1 · 20.1).</div>
  <div class="warn"><b>모바일에서도 그리드는 세로로 쌓지 않고 가로 표 그대로입니다.</b>
  <code>gvwbox</code>는 화면 끝까지 늘어나고, 그리드는 <b>컬럼 수 × 90</b>으로 넓어져
  <b>375를 넘는 만큼이 실행 시 가로 스크롤</b>이 됩니다. <b>375 초과가 버그가 아닌 유일한
  예외</b>입니다(5.1). Figma에서 폭은 그리드 레이어 하나에만 주고, 셀은 채우기로 둡니다.</div>
  <div class="note"><b>화면에 실제로 나오는 열 폭은 CSS가 다시 정합니다</b> —
  <b>44 · 88 · 152 · 채우기</b>의 4단계입니다. 그래서 Figma의 90은 「그리는 기준값」이고 화면 값과
  다릅니다. 행 높이는 <b>42</b>, 셀 글자는 <b>16 Medium</b>입니다(11.1 · 21.1).
  <b>셀 내부 위젯은 종류 정보만 전달됩니다</b> — 체크박스·셀렉트·달력을 두면 변환기가 <b>컬럼 타입</b>만
  읽고 실제 위젯은 프레임워크가 만듭니다. 작성 범위는 <b>셀마다 판정용 아이콘 하나</b>입니다(11.2).</div>`,

  tree: `<p>트리는 상위·하위 관계를 펼치고 접는 목록입니다. 두 항목이 있고 <b>구조는 하나</b>입니다 —
  <code>tvwbox</code> 그룹 › 트리(<code>tvw</code>) › <code>node</code> 프레임. 12.2는 클래스 토큰
  하나만 다릅니다.</p>
  <div class="warn"><b>계층은 중첩으로 만듭니다 — 그리드 드릴다운과 반대입니다.</b>
  11.4는 행을 같은 레벨에 두고 <b>텍스트 들여쓰기 값</b>으로 계층을 계산하지만, 트리는
  <b>노드의 중첩 구조</b>만 봅니다. 트리를 들여쓰기로 그리면 전부 1단계가 되고, 드릴다운을 중첩으로
  그리면 그 행이 결과에서 빠집니다.</div>
  <div class="note"><b>노드 이름의 두 번째 조각은 <code>node</code>와 완전일치여야 합니다.</b>
  어긋나면 그 프레임은 노드가 아니게 되어 <b>하위 노드까지 통째로</b> 빠집니다(12.1 · 20.1).</div>
  <div class="note"><b>트리 높이는 Figma에서 잰 값이 그대로 실립니다.</b> 클래스가 있어도 예외적으로
  나가는 값이라 표시 높이는 <b>Figma에서 정합니다.</b> 화면 높이에 맞춰 크게 그리면 375 화면에서
  지나치게 긴 트리가 되므로 <b>템플릿 기준 300</b>이 무난합니다. 모바일 치수는 노드 한 줄 28 ·
  글자 16 · 형제 노드 사이 8입니다(12.1).</div>
  <div class="warn"><b>노드 안에 이름에 <code>check</code>가 든 레이어가 하나라도 있으면 트리 전체에
  체크박스가 켜집니다.</b> 특정 노드에만 켤 수는 없으며, 의도치 않은 활성화도 이 규칙 때문입니다.</div>`,

  button: `<p>버튼과 트리거는 <b>형태와 구조가 같습니다.</b> 클래스 조합(<code>btn_cm</code> → 변형 →
  <code>icon</code> → <code>disabled</code>), 아이콘을 클래스가 정하는 방식, 안에 텍스트 레이어 하나를
  두는 방식까지 같습니다. 이름의 <b>두 번째 조각</b>이 <code>button</code>인지 <code>trigger</code>인지로
  갈립니다.</p>
  <div class="warn"><b>차이는 하나 — 배경색을 누가 정하는가입니다.</b>
  <b>버튼은 클래스(CSS)가, 트리거는 Figma에서 칠한 채움색이 정합니다.</b> 트리거는 그 색이 인라인으로
  나가 CSS보다 우선하기 때문입니다. 그래서 강조 버튼은 <code>fill</code> 토큰으로 쓰고,
  <b>트리거는 비활성 등 상태별 색까지 Figma에서 구분해 칠합니다.</b> 두 컴포넌트는 화면에서 구분되지
  않으므로 <b>검수는 레이어 이름과 결과 XML</b>로 합니다(13.2 · 19.2).</div>
  <div class="warn"><b>모바일에서는 크기를 고르는 것이 아니라 「어디에 놓였는가」가 규격을 정합니다.</b>
  <b>자기 행을 통째로 차지하면</b> 좌우 여백 20 · <b>모서리 8</b> · 아이콘 20,
  <b>다른 요소와 같은 줄에 얹히면</b> 좌우 여백 12 · <b>모서리 6</b> · 아이콘 16입니다.
  <b>높이는 어느 쪽이든 48</b>이며, 더 큰 버튼은 클래스를 적을 때만 나옵니다 —
  <code>md</code> 52 · <code>lg</code> 56 (둘 다 모서리 8). <b>클래스를 적으면 배치보다 우선합니다</b>(13.1).</div>
  <div class="note"><b>적용할 아이콘은 클래스가 정합니다.</b> 변형 토큰(<code>search</code>·<code>save</code>
  등)을 이름에 적어야 화면에 그 아이콘이 나옵니다. 목록에 없는 버튼에 <code>icon</code>만 붙이면
  <b>빈 사각형</b>이 됩니다 — 쓸 수 있는 아이콘 클래스는 7.2와 부록 21.5에 있습니다.
  <b>Figma에는 아이콘을 그립니다</b> — 결과에 안 나가는 것과 안 그려도 되는 것은 다릅니다.
  아이콘 전용 버튼은 <code>icon</code> 토큰을 넣고 <b>텍스트 레이어를 두지 않으며 48 × 48</b>입니다.</div>
  <div class="note"><b>상태는 클래스가 아니라 속성으로 나갑니다.</b> 이름에 <code>disabled</code>를 적으면
  결과에 <code>disabled="true"</code>가 붙고 <b>클래스에서는 빠집니다.</b>
  <b>버튼·트리거가 처리하는 상태는 <code>disabled</code> 하나뿐</b>이며,
  <code>req</code>·<code>error</code>·<code>readonly</code>는 클래스에서 빠지기만 하고 결과에 나가지
  않습니다 — 그 셋은 폼 필드(15장)에서만 전달됩니다.</div>`,

  accordion: `<p>아코디언은 제목을 눌러 본문을 펼치고 접는 목록입니다. 구조는
  <code>acdbox</code> 그룹 › 아코디언(<code>acd</code>) › <code>panels</code> 프레임이며,
  패널마다 <code>paneltitle</code>(제목)과 <code>panelcontent</code>(본문)를 둡니다.</p>
  <div class="warn"><b>본문은 텍스트 한 줄만 전달됩니다 — 9장 탭과 반대입니다.</b>
  탭 본문은 작성한 구조가 유지되는 유일한 자리였지만, 아코디언 본문은
  <b><code>panelcontent</code> 안 첫 텍스트 하나</b>로 줄어듭니다. 표·버튼·여러 문단을 그려도
  결과에 반영되지 않습니다. <b>본문이 복잡한 화면에는 아코디언이 아니라 탭을 씁니다</b>(14.1 · 20.1).</div>
  <div class="note">제목·본문·패널의 판정 기준은 모두 <b>이름의 두 번째 조각</b>이고 <b>완전일치</b>입니다 —
  <code>panels</code> · <code>paneltitle</code> · <code>panelcontent</code>. 단수형(<code>panel</code>)이나
  숫자가 붙은 이름(<code>panel1</code>)은 인식되지 않습니다.</div>
  <div class="note"><b>모바일 아코디언은 테두리도 모서리도 없는 흰 면입니다.</b> 패널 사이는 20,
  제목 줄은 높이 24 · 좌우 20 · 글자 16 Bold입니다. 접힌 패널은 본문 없이 그리되 <b>화살표는
  그려 둡니다</b>(14.1).</div>`,

  form: `<p>입력폼은 10장 입출력 테이블과 <b>표 구조가 같습니다</b> — 행은 <code>tr</code>,
  라벨셀은 <code>th</code>, 값셀은 <code>td</code>입니다. 본 장에서 더 다루는 것은 <b>값셀에 두는
  필드의 명명 방식</b>입니다.</p>
  <div class="note"><b>필드 종류는 두 번째 조각이 정하고, 상태는 이름의 어느 자리에 있어도 걸립니다.</b>
  <code>disabled</code>(<code>dis</code>) · <code>readonly</code>(<code>ro</code>) ·
  <code>req</code>(<code>required</code>) · <code>error</code>(<code>err</code>) —
  <b>조각 단위 완전일치</b>라 <code>disabled2</code>처럼 붙여 쓰면 걸리지 않습니다.
  비활성·읽기전용은 <b>속성</b>으로, 필수·에러는 <b>클래스</b>로 나갑니다. 모바일의 필수 표시는
  파란 점이 아니라 <b>별표</b>입니다(21.1).</div>
  <div class="warn"><b>폼 필드는 이름의 클래스가 결과에 나가지 않습니다.</b> 변환기가 필드별 속성 세트를
  만들어 내보내기 때문이며, 예외는 <code>req</code>·<code>error</code> 둘뿐입니다.
  <b>읽기전용도 셀렉트·멀티셀렉트·체크박스·라디오·업로드에서는 나가지 않으며</b>, 그 다섯은 비활성으로만
  잠급니다(15.1).</div>
  <div class="warn"><b>모바일 필드는 한 줄을 다 씁니다 — 폭을 Figma에서 맞추려 하지 않습니다.</b>
  높이 <b>48</b> · 모서리 <b>8</b> · 글자 <b>16</b> · 폭 <b>335</b>가 기본이고, 값칸 안 필드가 남는 폭을
  나눠 갖도록 CSS가 다시 정합니다. 원본 XML에 박힌 PC 폭(120 · 148 등)도 화면에는 나타나지 않습니다.
  <b>잰 폭이 결과 XML에 실리는 경우는 한 칸에 요소가 둘 이상일 때의 인풋 하나뿐</b>입니다(5.4) —
  15.3이 그 사례입니다. 가로로 나열할 때는 <b>한 줄에 최대 3개</b>입니다.</div>`,

  calendar: `<p>날짜를 입력받거나 보여 주는 컴포넌트는 <b>두 가지</b>이고 이름의 <b>두 번째 조각</b>으로
  갈립니다 — 칸 안에 두는 <b>날짜 입력칸</b>(<code>inputcalendar</code>)과 화면에 펼치는
  <b>달력</b>(<code>calendar</code>)입니다. 세 번째 항목은 별개의 컴포넌트가 아니라, 펼친 달력의
  년·월 컨트롤을 브라우저 기본 형태로 바꾸는 이름 토큰(<code>native</code>)입니다.</p>
  <div class="warn"><b>이 계열은 세 번째 조각이 클래스가 아니라 변형 지정자입니다</b>(2.5).
  <code>yearMonthDate</code> · <code>native</code>처럼 씁니다. 이 자리에 CSS 클래스를 적으면
  클래스로 새어 나가 정본과 어긋납니다. <b>날짜 단위를 지정할 수 있는 것은 펼친 달력뿐</b>이며,
  <b>날짜 입력칸은 이름과 무관하게 연·월·일로 고정</b>입니다(16.1 · 16.2).</div>
  <div class="note"><b>모바일 값</b> — 날짜 입력칸은 <b>높이 48짜리 폼 필드</b>가 되고 <b>폭은 칸을
  채웁니다</b>(PC의 120px 고정과 다릅니다). 펼친 달력은 폭 <b>335</b> · 모서리 <b>16</b>이고,
  <b>주말도 평일과 같은 색</b>이며 요일은 한글입니다. <b>모서리는 CSS에 적힌 4가 아닙니다</b>(21.1).
  달력 두 개는 가로로 나란히 두지 않고 <b>세로로 쌓습니다</b>(16.3).</div>
  <div class="note"><b>펼친 달력은 내부 구조가 결과에 반영되지 않습니다</b>(4장의 목록에 포함).
  그렇다고 빈 프레임으로 두지는 않으며, 작성 범위는 16.2의 「그리는 기준선」에 있습니다.
  연 이동 화살표와 확인·닫기 버튼은 화면에서 숨겨지므로 <b>그리지 않습니다.</b></div>`,

  schedule: `<p>스케쥴 캘린더는 일정을 월 단위로 보여 주는 달력입니다. 16장의 두 컴포넌트와 이름이
  비슷하지만 <b>별개의 컴포넌트</b>이며, 두 번째 조각은 <code>schedulecalendar</code>입니다.
  감싸개는 <code>calendarbox</code>를 씁니다 — 다른 이름을 주면 변환기가 하나 더 만들어 이중으로
  감쌉니다.</p>
  <div class="warn"><b>내부 구조가 결과에 반영되지 않습니다</b>(4장의 목록에 포함).
  날짜 격자와 일정 막대를 그려도 결과에는 태그 하나만 나가고, 보여 줄 일정은 화면 실행 후 데이터가
  정합니다. <b>작성 범위는 툴바와 요일 머리, 주 한 줄까지</b>입니다 — 다섯 줄을 다 그리는 것이 이
  컴포넌트에서 가장 불필요한 작업입니다(17.1).</div>
  <div class="note"><b>모바일 툴바는 PC와 다릅니다.</b> 왼쪽은 <b>「오늘」 하나</b>이고(PC는 이동 버튼까지
  셋), 이전·다음 버튼은 화면에서 숨겨지므로 <b>그리지 않습니다.</b> 보기 전환은 붙은 세그먼트가 아니라
  <b>낱개 버튼 셋</b>이며, 달력은 <b>카드 끝까지 풀폭</b>으로 놓입니다(17.1).</div>`,

  widget: `<p>본 장은 앞선 장에 들어가지 않는 네 가지를 다룹니다 — 메시지 · 위젯 컨테이너 ·
  떠 있는 레이어 · 진행 단계.</p>
  <div class="warn"><b>넷 중 둘은 내부 구조가 결과에 반영되지 않습니다</b> —
  위젯 컨테이너 · 떠 있는 레이어. 4장의 목록에 든 컴포넌트입니다.
  <b>단, 빈 프레임으로 두지 않습니다.</b> 인수자가 확인하는 것은 Figma 화면이므로 외형은 알아볼 수
  있는 수준까지 그립니다 — <b>항목마다 「그리는 기준선」에 작성 범위를 적었습니다</b>(18.2 · 18.3).</div>
  <div class="note"><b>나머지 둘은 작성한 내용이 전달됩니다.</b> 메시지는 클래스만으로 만드는 요소이며
  (<code>txt_info</code> · <code>msgbox</code> · <code>listbox</code>), 진행 단계는 <b>단계 개수 · 이름 ·
  상태가 모두 전달되는</b> 본 장의 유일한 항목입니다 — 상태는 이름의 <code>finish</code>·<code>on</code>이
  정합니다(18.4).</div>
  <div class="note"><b>모바일 값</b> — 위젯은 3칸이 아니라 <b>한 줄에 하나씩 세로로</b> 쌓이고(18.2),
  떠 있는 레이어는 XML에 폭 300이 나가지만 <b>화면에서는 카드 안쪽 폭 335</b>로 넓어집니다(18.3).
  진행 단계는 <b>화면 끝까지 풀폭</b>으로 놓여 다섯 칸이 폭을 똑같이 나눠 갖고(18.4),
  메시지 글자는 14에서 <b>16</b>으로 올라갔습니다(18.1).</div>`,
};
