# 2026-04-27 작업 이력

## 주요 변경사항

### 1. 파일 등록/선택/refresh 플로우 전면 개편
- 등록 시 노드 트리(figma_file_data) + SVG 이미지 로컬 저장
- 선택 시 Figma API 호출 없이 DB에서 로드 (synthetic file로 렌더)
- refresh 시 변경 감지(JSON 비교) → hasChanges=true일 때만 DB/이미지 갱신
- 이미지 저장 방식: PNG scale=2 → SVG scale=1 변경

### 2. 버전 관리 시스템
- figma_files 테이블에 version, xmlFilename 컬럼 추가
- refresh 변경 감지 시 "버전 올리시겠습니까?" 컨펌 → bumpFigmaFileVersion
- 에디터 헤더에 v## 뱃지 표시
- XML/Spec Export 시 DB 버전 기준 폴더 구조 ({filename}/v{NN}/{filename}.xml)

### 3. XML Export 파일명 기억
- 최초 저장 시 모달로 파일명 입력 → figma_files.xmlFilename에 저장
- 이후 저장은 모달 생략, 매번 파일명 확인/변경 가능

### 4. 스펙 문서 기능 확장
- SPA 내부 view로 전환 (URL /spec 제거, main.tsx 단일 App 렌더)
- 등록 시 스펙 트리 + _spec.svg 저장 (매핑 파일 identity)
- 새로고침: 변경 감지 → _spec.svg 재저장 + 매핑 파일 버전업 + autoMapSpec 재실행
- 삭제 시 figma_file_data + _spec.svg 동반 제거
- 토스트 메시지 (alert 대체)
- v02+ 스펙 생성 시 이전 버전 누적 참조 → 변경분만 작성

### 5. VS Code Extension 구축
- packages/extension/ 독립 패키지
- WebviewPanel에 React 클라이언트 빌드 산출물 임베드
- fetch monkey-patch (API → 서버), MutationObserver (정적 자산 경로 재작성)
- Preview Server (엔진 + 워크스페이스 파일 서빙, DeploySource 반영)
- 사이드바 프로젝트 TreeView (추가/삭제/이름변경/새로고침)
- 파일 열림 → 사이드바 닫기, 프로젝트 이동 → 사이드바 열기
- CSS 파일 선택: VS Code 다이얼로그 → 워크스페이스 상대경로 → <link> 로드
- confirm/alert → 커스텀 다이얼로그 (DialogContext) — webview sandbox 대응
- portMapping + asExternalUri로 로컬 서버 접근

### 6. UI/UX 개선
- NodeTree 선택 시 자동 스크롤 제거 (상위 펼치기만 유지)
- 설정에 "XML 변환 시 인라인 스타일 포함" 체크박스 추가
- figma-style.ts: button/select fallback (transparent bg/border), TRBL 4-value shorthand
- textbox white-space:nowrap 조건 개선 (textAutoResize === 'HEIGHT' + 단일라인)
- visible=false 노드 스펙 자동매핑 텍스트 수집에서 제외
- SVG→PNG 변환 (canvas) — Claude API SVG 미지원 대응

### 7. widgetContainer 컴포넌트
- widget, widgetItem, widgetTitle, widgetContent 4개 가상 컴포넌트
- widgetContainer → XML + addWidgets JS 코드 자동 생성 (scwin.onpageload 내부)
- widgetTitle: textbox 매핑된 노드의 텍스트만 title로 사용
- 자동 ID 채번: widget=wg_01~, widgetContent=wg_01_wc_01~ (부모별 01부터)
- 다중선택 시 개별 채번
- cols 속성, widgetStyle CSS 주입, initScript postMessage 전달

### 8. 서버 API 추가
- /api/figma-proxy/* — Figma API 프록시 (Extension/운영 환경용)
- /api/figma-files/image — 이미지 저장/조회/삭제 (suffix 파라미터)
- /api/figma-files/bump-version — 버전 증가
- /api/figma-files/xml-filename — XML 파일명 조회/저장
- /api/export-spec — 스펙 마크다운 저장
- /api/export-spec/prior — 이전 버전 스펙 조회
- /api/auto-map-spec — data URL 이미지 지원
- CORS Allow-Headers에 X-Figma-Token 추가
- WebSquare config-global.js 정적 서빙 (미리보기 iframe용)

### 9. 엔진 업그레이드
- wba_B1365 → wbd_B1548 (index.html, preview.html, websquare-preview.html)

### 10. 스펙 생성 프롬프트 개선
- TODO 섹션 추가 (팝업연결, 화면전환, API연동 등)
- interface-metadata: initialData 다건이면 type=dataList
